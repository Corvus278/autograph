/**
 * Версия приложения вида `<мажор>.<минор>.<номер коммита>`: мажор и минор
 * берутся из `package.json`, третье число — счётчик коммитов. Значение
 * подставляет сборка, поэтому его не пересчитать в рантайме и не разойтись с
 * тем, из чего собран лежащий на раздаче код.
 *
 * Пустая строка — сборки не было (dev-сервер, тесты) или история git
 * недоступна: показывать в этих случаях нечего.
 */
export const APP_VERSION = typeof __APP_VERSION__ === 'undefined' ? '' : __APP_VERSION__;
