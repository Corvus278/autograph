/**
 * Источник случайности: возвращает число в диапазоне `[0, 1)`. Ядро принимает
 * его снаружи, чтобы отрисовка на одном seed повторялась — иначе любой
 * перерендер React менял бы почерк.
 */
export type RandomSource = () => number;
