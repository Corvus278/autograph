export type ViewportSize = {
  /**
   * Ширина области в CSS-пикселях без полосы прокрутки.
   */
  width: number;

  /**
   * Высота области в CSS-пикселях без полосы прокрутки.
   */
  height: number;
};
