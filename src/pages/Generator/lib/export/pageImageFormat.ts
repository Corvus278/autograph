/**
 * Формат, в котором страница уходит пользователю: фотография с потерями, а не
 * растр без потерь. Страница притворяется снимком настоящего листа, а идеально
 * чистый растр читается как отрисовка.
 */
export const PAGE_IMAGE_MIME = 'image/jpeg';

/**
 * Расширение файла страницы.
 */
export const PAGE_IMAGE_EXTENSION = 'jpg';
