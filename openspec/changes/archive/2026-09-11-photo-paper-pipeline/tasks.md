## 1. Спайки

- [x] 1.1 Спайк S1: прочитать контуры глифов встроенного связного шрифта, сместить внутренние точки шумом, зафиксировав точки входа и выхода штриха, и отрисовать слово — проверить по увеличенному снимку, что соединения между буквами не разорваны
- [x] 1.2 Спайк S1: разложить композитные глифы (`й`, `ё`) перед деформацией — проверить, что диакритика не отрывается от буквы
- [x] 1.3 Зафиксировать в `design.md` итог S1: остаётся ли решение D5 или включается альтернатива A; проверка — раздел «Decisions» обновлён с датой и результатом
- [x] 1.4 Спайк S2: на четырёх реальных фотографиях тетрадей (две в клетку, две в линейку) найти шаг разлиновки автокорреляцией профиля яркости — проверить, что найденный шаг совпадает с измеренным вручную на всех четырёх
- [x] 1.5 Спайк S2: добавить свип угла в диапазоне ±2° с максимизацией контрастности профиля — проверить, что на намеренно повёрнутом снимке шаг находится с той же точностью, что и на ровном
- [x] 1.6 Замерить время анализа фотографии в полном разрешении и на уменьшенной копии — зафиксировать в `design.md`, нужен ли перенос анализа с CPU (решение D6)

## 2. Модель листа

- [x] 2.1 Ввести типы семьи и экземпляра листа с канонической разлиновкой, наклоном, нормировкой и фазой — проверить типами и юнит-тестом на нормировку шага экземпляра к канону семьи
- [x] 2.2 Реализовать поиск разлиновки и полей по фотографии — проверить юнит-тестами на подготовленных изображениях с известным шагом и известными полями
- [x] 2.3 Реализовать определение угла наклона свипом — проверить юнит-тестом на изображении, повёрнутом на известный угол
- [x] 2.4 Реализовать извлечение поля освещения и карты текстуры с признаком пригодности — проверить юнит-тестами: неравномерно освещённый образец даёт пригодное поле, равномерный — непригодное
- [x] 2.5 Реализовать ручной ввод разлиновки и полей как запасной путь — проверить компонентным тестом: при неудачном автоопределении показывается форма ручного ввода, фотография не отбрасывается
- [x] 2.6 Реализовать пометку листа как чистого (без разлиновки) с ручным шагом строк — проверить компонентным тестом
- [x] 2.7 Реализовать локальное хранение пользовательских листов и их характеристик — проверить тестом: после перезагрузки лист доступен с теми же характеристиками, повторный анализ не запускается
- [x] 2.8 Реализовать удаление пользовательского листа — проверить компонентным тестом

## 3. Пресет-пак

- [x] 3.1 Снять телефоном фотографии пустых листов: не менее четырёх в клетку и четырёх в линейку — проверить, что каждый снимок обрезан по краям листа и имеет неравномерное освещение
- [x] 3.2 Собрать скрипт сборки, считающий характеристики предустановленных экземпляров заранее — проверить, что артефакт с характеристиками появляется рядом с фотографиями и не пересчитывается в рантайме
- [x] 3.3 Завести две предустановленные семьи с каноническими разлиновками и нормировать экземпляры — проверить тестом: у всех экземпляров семьи нормированный шаг совпадает с каноном
- [x] 3.4 Проверить, что обе семьи доступны при первом открытии генератора без загрузки своих листов — компонентный тест

## 4. Автокалибровка

- [x] 4.1 Реализовать снятие метрик шрифта (высота строчных, подъём) с кэшированием на шрифт — проверить юнит-тестом на измерителе-модели
- [x] 4.2 Реализовать вывод размера шрифта, межстрочного интервала, верхнего и левого отступов и ширины блока из разлиновки семьи и метрик шрифта — проверить юнит-тестами
- [x] 4.3 Проверить попадание базовых линий на разлиновку в браузере: story на живом измерителе, отклонение не более десятой доли шага
- [x] 4.4 Проверить, что смена шрифта сохраняет попадание на линии без правки настроек — story на живом измерителе
- [x] 4.5 Реализовать наклон блока текста по углу экземпляра — проверить компонентным тестом, что применённый угол равен углу экземпляра
- [x] 4.6 Превратить существующие слайдеры геометрии в поправку поверх вычисленных значений, с сохранением при смене экземпляра и сбросом — проверить тестами стора
- [x] 4.7 Проверить независимость пагинации от экземпляра: юнит-тест на измерителе-модели сравнивает разбивку при разных экземплярах одной семьи

## 5. Рендерер на canvas

- [x] 5.1 Реализовать отрисовку страницы на canvas: фон-фотография, строки и слова с их трансформациями — проверить компонентным тестом на совпадение состава отрисованного с раскладкой
- [x] 5.2 Перевести предпросмотр на тот же рендерер в низком разрешении — проверить тестом, что предпросмотр и полный снимок отличаются только масштабом
- [x] 5.3 Реализовать зеркалирование чётных страниц с выводом отступа блока из отражённой разлиновки — проверить компонентным тестом на второй странице
- [x] 5.4 Убрать отдельную настройку отступа чётных страниц — проверить, что смена семьи пересчитывает отступ без участия пользователя
- [x] 5.5 Удалить DOM-отрисовку страницы, снятие снимка через сериализацию и зависимость на неё — проверить, что сборка проходит и зависимость исчезла из `package.json`
- [x] 5.6 Перенести stories предпросмотра на canvas — проверить прогоном stories в браузере
- [x] 5.7 Переснять скриншотные эталоны в docker-образе и зафиксировать порог расхождения — проверить повторным прогоном скриншотных тестов
- [x] 5.8 Проверить, что в снимок не попадают элементы интерфейса — компонентный тест

## 6. Чернила

- [x] 6.1 Реализовать вариативность контуров глифов по решению из задачи 1.3 — проверить юнит-тестом, что два вхождения одной буквы дают разные контуры при одном шрифте
- [x] 6.2 Реализовать переключатель вариативности — проверить тестом, что при выключении отрисовываются исходные контуры шрифта
- [x] 6.3 Проверить сохранение связности почерка при включённой вариативности — скриншотный тест на слове из связного шрифта
- [x] 6.4 Реализовать модуляцию чернил полем освещения и текстурой листа шейдером — проверить тестом: в слабо освещённой части листа чернила темнее, чем в ярко освещённой
- [x] 6.5 Реализовать запасное синтетическое освещение для листов с непригодным полем — проверить тестом, что чернила не остаются равномерными
- [x] 6.6 Перевести растеризацию на JPEG с рендером в повышенном разрешении — проверить тестом формат файла и ширину изображения не менее утроенной ширины предпросмотра

## 7. Рецепт прогона

- [x] 7.1 Ввести рецепт прогона, выводящий из одного seed экземпляры листов по страницам, цвет чернил, параметры почерка, вариативности и оптики — проверить юнит-тестом на состав рецепта
- [x] 7.2 Проверить воспроизводимость: юнит-тест, что два рецепта с одним seed совпадают полностью
- [x] 7.3 Реализовать выбор цвета чернил из палитры правдоподобных с разбросом и уважение вручную заданного цвета — проверить юнит-тестами обоих случаев
- [x] 7.4 Реализовать правило «соседние страницы не получают один экземпляр» с корректной работой на семье из одного экземпляра — проверить юнит-тестами обоих случаев
- [x] 7.5 Перевести правку текста на сохранение рецепта, а смену рецепта — на явное действие — проверить тестами стора
- [x] 7.6 Убедиться, что в слое отрисовки не осталось источников случайности помимо рецепта — проверить прогоном одной страницы дважды на совпадение попиксельно

## 8. Пачка

- [x] 8.1 Вынести отрисовку страниц в Web Worker на внеэкранный canvas — проверить тестом, что интерфейс отвечает во время отрисовки в полном разрешении
- [x] 8.2 Реализовать потоковую генерацию пачки: страница кодируется и освобождается до отрисовки следующей — проверить генерацией пачки из двадцати страниц без падения по памяти
- [x] 8.3 Реализовать упаковку пачки в архив с сортируемыми именами по номеру страницы — проверить тестом состава и имён файлов в архиве
- [x] 8.4 Реализовать прогресс с числом готовых и общим числом страниц — проверить компонентным тестом
- [x] 8.5 Реализовать отмену выгрузки — проверить тестом, что генерация прекращается, архив не скачивается, приложение работоспособно
- [x] 8.6 Реализовать устойчивость к ошибке отдельной страницы — проверить тестом, что архив содержит остальные страницы, а номер неудавшейся показан
- [x] 8.7 Сохранить сохранение одной текущей страницы отдельным действием — проверить компонентным тестом
- [x] 8.8 Проверить, что пачка отражает рецепт: разные экземпляры листов в архиве, чётные зеркальны, повторный прогон с тем же seed совпадает попиксельно

## 9. Завершение

- [x] 9.1 Обновить `CLAUDE.md`: разделы «Как работает генератор», «Ассеты», «Тесты» — проверить, что описание соответствует новому пути отрисовки и модели листов
- [x] 9.2 Прогнать `npm run lint` и `npm test` — ноль ошибок и предупреждений в новом коде
- [x] 9.3 Прогнать `npm run test:stories` и `npm run test:visual` — проверить, что оба слоя зелёные на переснятых эталонах
- [x] 9.4 Сквозная проверка: загрузить свою фотографию тетради, сгенерировать пачку из десяти страниц, убедиться, что текст лёг на линии, чётные зеркальны, листы различаются
