## 1. Эталоны до правок

- [x] 1.1 Снять эталон пресет-пака до первой правки кода: по каждому листу из `public/paper/profiles.json` (`jq`) — `step`, `firstLinePhase`, `skewAngle`, `margins`, `marginLineX`, `marginLineSide`, наибольшее `|offsets|` изгиба, `lighting.contrast`, `lighting.isUsable`, `texture.amplitude`. Проверка — таблица записана в журнал прогона вместе с коммитом, на котором снята.
- [x] 1.2 Снять базовое время `detectRuling` на синтетике 3000×4000 (`tests/synthetic-sheet-detect-timing.test.ts`) до первой правки детектора. Проверка — время и коммит записаны в журнал.

## 2. Модель: контур листа и перспектива

- [x] 2.1 Добавить в `lib/paper/paper.types.ts`:
  - типы `SheetOutline` (четыре угла `{ x, y }` в пикселях кадра) и `RulingPerspective` (`originX`, `originY`, `convergenceX`, `convergenceY`);
  - обязательные `outline: SheetOutline | null` и `perspective: RulingPerspective | null` в `SheetRuling`, необязательные — в `SheetRulingSource`, `perspective` — в `RulingDetection`.

  Проставить `null` во всех литералах разлиновки и детекции: `detectRuling.ts` (`toMissingDetection` и результат), `mirrorSheetRuling.ts`, `paperSheetJson.ts`, `useSheetImport.ts`, `PaperGroup.tsx`, `config/paperFamilies.ts`, stories `BaselineFit`/`RasterRuling`, `tests/helpers/paper-family.ts`, тесты — по выводу `npm run typecheck`. Проверка — `npm run typecheck` и `npm test` проходят без правки ожиданий.
- [x] 2.2 Написать `resolveSheetBounds(outline, width, height)` в `lib/paper/`: вписанный прямоугольник отступами от краёв кадра, `null` — нулевые отступы. Проверка — юнит-тесты:
  - `null` даёт нули;
  - у четырёхугольника, повёрнутого на 1,5°, с перспективным сужением 3 % каждая из 400 точек сетки внутри прямоугольника лежит внутри контура;
  - у прямоугольного контура отступы равны его сторонам.
- [x] 2.3 Написать в `lib/paper/rulingPerspective.ts` координату вдоль линий `U(ruling, x, y)`, обратную высоту `Y(ruling, x, U)` и производные `∂Y/∂x` при постоянной `U` и `∂Y/∂U` по формулам design («Перспектива — дробно-линейная координата вдоль линий»). Проверка — юнит-тесты:
  - при `perspective: null` `U = y − x·tgθ` и `Y = U + x·tgθ` точно;
  - `Y(x, U(x, y)) = y` с точностью 1e-6 на сетке точек кадра 3000×4000 при `p, q` дрейфа 8 %;
  - производные совпадают с конечными разностями;
  - `U` отражённой разлиновки в `(W − x, y)` равна `U(x, y) + W·tgθ`.
- [x] 2.4 Перевести `buildSheetRuling` на `buildSheetRuling(source, frame)`:
  - фолбэк полей — полтора шага от сторон `resolveSheetBounds`;
  - верхнее поле фолбэком — `Y(0, ceilToLine(U(0, верх) + 1,5·step))`;
  - перенос `outline` и `perspective`; перспектива — `null` при `step ≤ 0`.

  Обновить вызовы в `paperFamilies.ts`, `PaperGroup.tsx`, `paperSheetJson.ts`, `useSheetImport.ts`, `build-paper-profiles.ts`. Проверка:
  - прежние тесты `paper-sheet-ruling` проходят без правки ожиданий;
  - новые юнит-тесты: контур даёт фолбэк от сторон прямоугольника, а не кадра; при перспективе верхнее поле фолбэком лежит на линии (`U(0, top)` кратна шагу от фазы с точностью 1e-6).
- [x] 2.5 Научить `mirrorSheetRuling` отражать перспективу (`originX′ = W − originX`, `convergenceX′ = −convergenceX`), контур (`x → W − x`, левые и правые углы меняются местами) и верхнее и нижнее поля через `Y(W, U(0, ·))`. Проверка — юнит-тест на синтетической разлиновке с наклоном 1°, дрейфом 6 % и несимметричным контуром: положение линий, восстановленное по отражённой разлиновке в пяти `x`, совпадает с отражёнными вручную линиями с точностью до сотой шага; углы контура отражены. Прежние тесты отражения проходят без правки. Двойным отражением не проверять.
- [x] 2.6 Перевести `sampleRulingBend` и `sampleRulingBendSlope` на `{ skewAngle, perspective }`: строки узлов — по `U`. Обновить вызовы в `renderPageToCanvas.ts` и тестах. Проверка — прежние тесты `paper-ruling-bend` проходят с `perspective: null`; новый тест: при перспективе точка на линии `k` получает смещение своей строки узлов, производная совпадает с конечной разностью вдоль линии.

## 3. Синтетика

- [x] 3.1 Расширить `tests/helpers/synthetic-sheet.ts`:
  - поверхность вокруг листа: контур-четырёхугольник со скруглёнными углами, яркость и зерно поверхности, полоса обложки у одной стороны, пятна спирали поверх края, соседний лист светлее самого листа за спиралью, мягкая виньетка к краям кадра;
  - перспектива: линии по `Y(x, U)` с заданными `p` и `q`, вместе с изгибом.

  Экспортировать эталонные контур и перспективу. Проверка — юнит-тест хелпера: точка за контуром имеет яркость поверхности, центр нарисованной линии `k` в пяти столбцах лежит на `Y(x, U_k) + d` с точностью полпикселя.

## 4. Контур и измерение внутри листа

- [x] 4.1 Написать `lib/paper/detectSheetOutline.ts` по design («Контур листа: четыре прямые по полосам кадра»). Проверка — юнит-тесты на синтетике 3000×4000:
  - стол с трёх сторон и спираль с соседним светлым листом с четвёртой: найденные стороны отстоят от эталонных не больше чем на 0,6 % стороны кадра; левая сторона — по внутреннему краю тени спирали, не дальше неё от листа;
  - лист повёрнут на 1,5° и сужен перспективой 3 %, углы скруглены;
  - обрезанный лист без поверхности, светлая поверхность, лист во весь кадр с виньеткой: все стороны — край кадра;
  - полоса стола только сверху: найдена одна верхняя сторона.
- [x] 4.2 Добавить `outline` в `extractLighting` и `extractTexture` по design («Свет и текстура только с бумаги»). Проверка — юнит-тесты:
  - без контура выход побитово совпадает с выходом до правки на трёх синтетических листах (эталон снят до правки);
  - равномерный лист с тёмной полосой стола по краю: `isUsable: false`, узлы у края листа отличаются от середины не больше чем на 0,01;
  - лист с боковым светом на столе: поле внутри контура и размах совпадают с полем вырезки по контуру ±0,02;
  - размах текстуры совпадает с размахом вырезки ±5 %.
- [x] 4.3 Написать `measureSheetPhoto(image, { kind, outline? })` в `lib/paper/`:
  - контур (свой или найденный) → вырезка → `detectRuling` → перевод фазы, полей, линии поля и изгиба в кадр;
  - свет и текстура с контуром;
  - неудача разлиновки отдаёт контур, свет и текстуру, а разлиновку — ненайденной;
  - `kind: 'blank'` разлиновку не ищет.

  Проверка — юнит-тесты на синтетике клетки на столе:
  - отрицательный контроль: `detectRuling` по кадру разлиновку не находит;
  - `measureSheetPhoto` находит шаг с точностью до сотой шага от прогона по вырезке, поля — ±1 px от переведённых полей вырезки;
  - восстановленные линии изогнутого листа на столе — не дальше 1/20 шага от эталона;
  - ненайденная сторона поля отступает полтора шага от стороны листа;
  - на листе без поверхности результат совпадает с `detectRuling(image)` и нынешним светом.
- [ ] 4.4 Перевести `useSheetImport` и `build-paper-profiles` на `measureSheetPhoto`. Проверка — компонентный тест `paper-group-import`: импорт синтетического листа на столе показывает найденные шаг и поля, контур в листе не `null`; прежние тесты импорта проходят.

## 5. Перспектива

- [x] 5.1 Написать `lib/paper/detectRulingPerspective.ts`: трасса по высоте в пяти полосах, подгонка Гаусса — Ньютона, проверка надёжности по design («Перспектива: измерение по выпрямленной копии», пп. 1–3). Проверка — юнит-тесты на синтетике:
  - дрейф шага 4 % и 8 % на линейке и на клетке, схождение линий 0,5° по ширине, с шумом: восстановленные линии не дальше 1/20 шага от эталона;
  - дрейф с изгибом 0,2 шага;
  - прогиб без перспективы со сменой знака дрейфа по ширине (как у IMG_1602): `null`;
  - отход меньше 1/40 шага: `null`;
  - дрейф 16 %: `null`;
  - половина линий закрыта пятном: трасса не проскальзывает на соседнюю линию (номера линий совпадают с эталоном).
- [x] 5.2 Встроить в `measureSheetPhoto` выпрямленную копию, второй проход и перевод в кадр (design, пп. 4–5), изгиб — по `U`. Проверка — юнит-тесты:
  - линейка с дрейфом 4 % на столе: верхнее поле на первой линии ±⅒ шага (у ровного прохода — ниже неё, отрицательный контроль), линии с изгибом восстановлены не дальше 1/20 шага в любой точке области;
  - клетка с дрейфом 4 %: поля ±½ шага от эталона;
  - второй проход не находит разлиновку (подменённая копия): результат равен ровному проходу с `perspective: null`.
- [x] 5.3 Замерить время `measureSheetPhoto` на синтетике 3000×4000 без поверхности и перспективы, со столом и с перспективой. Проверка — числа против базы 1.2 записаны в журнал; без перспективы не дольше двух баз, иначе остановиться и искать лишний проход.

## 6. Раскладка и отрисовка

- [x] 6.1 Перевести `resolveFirstLine` и `deriveTextHeight` на `U(0, ·)`; добавить перспективу в отпечаток листа `model/measureLayout.ts`. Проверка:
  - прежние тесты калибровки и пагинации проходят без правки;
  - на листе с перспективой базовая линия первой строки лежит на линии не выше верхнего поля;
  - базовая линия последней строки у левого края кадра не ниже нижнего поля и выше него меньше чем на шаг строк;
  - смена перспективы меняет ключ кэша раскладки.
- [x] 6.2 Обобщить сдвиг точек в `renderPageToCanvas.ts` до `Δ = Y + d − P.y`; режим — при `perspective !== null || bend !== null`; побуквенный путь — сдвиг и поворот по `∂(Y + d)/∂x`. `RenderGeometry.perspective` заполняет `buildPageRenderParams`. Проверка — тесты:
  - лента вызовов листа без перспективы и изгиба совпадает с эталоном (прежние тесты детерминизма);
  - на листе с дрейфом 6 % и наклоном 1° низ контуров базовой линии в пяти точках каждой строки не дальше 0,1 шага от линии, на нечётной и зеркальной странице, со шрифтом с контурами и без;
  - число связных областей чернил слова совпадает с тем же словом на ровном листе.
- [x] 6.3 Проверить, что текст остаётся на листе. Проверка — тест: на синтетическом листе на столе с несимметричным контуром верх бокса первой строки и низ бокса последней на нечётной и зеркальной странице лежат внутри контура отражённой или исходной фотографии.

## 7. Данные и интерфейс

- [ ] 7.1 Читать `outline` и `perspective` в `model/paperSheetJson.ts` с проверками из design («Хранение, JSON, профили»). Проверка — юнит-тесты `paper-sheet-json`: отсутствие, не четыре точки, нечисловые значения, `1 − a·q ≤ 0` на кадре — `null`; запись своего листа с контуром и перспективой переживает сохранение и чтение (`stored-user-sheets`).
- [ ] 7.2 Сделать `PaperGroup/SheetBoundsForm/` и хук `useSheetRemeasure`; подключить в `PaperGroup` у своего листа. «Применить разлиновку» переносит `outline` всегда, `perspective` — при том же условии, что `bend`. Проверка — компонентные тесты:
  - начальные значения — отступы вписанного прямоугольника, округлённые до сотых;
  - перемер заменяет разлиновку, свет и текстуру, сохраняя `id`, `label`, `src` (декодер подменён синтетикой на столе), и перетирает ручные поля;
  - границы, оставляющие меньше четверти высоты, дают сообщение, лист не меняется;
  - правка одних полей сохраняет перспективу и контур; правка шага снимает перспективу и изгиб;
  - после перемонтирования стора лист с тем же контуром.
- [ ] 7.3 Написать `scripts/measure-sheet.ts` и `npm run measure:sheet -- <путь>` по образцу `build:paper`: печать контура (найденные стороны, углы), вида, шага, фазы, наклона, перспективы (шаг у верхней и нижней линии области, дрейф в процентах), полей с пометкой фолбэка, линии поля, изгиба, света и времени. Добавить те же поля в отчёт `build-paper-profiles`. Проверка — скрипт печатает отчёт по `public/paper/grid/1.jpg`: «контур: нет», шаг совпадает с артефактом.
- [ ] 7.4 Пересобрать `public/paper/profiles.json` (`npm run build:paper`) и сверить с эталоном 1.1:
  - у всех листов `outline: null`, свет и размах текстуры те же;
  - у клетки `perspective: null`, шаг, фаза, поля и линия поля ±1 px;
  - у линейки записаны перспектива и сдвиг полей, фазы и изгиба; каждое расхождение больше пикселя разобрано в журнале.

  Проверка — `npm test` (включая `paper-profiles`) проходит на новом артефакте, журнал заполнен.

## 8. Stories, эталоны, документация, приёмка

- [ ] 8.1 Перевести арифметику `BaselineFit`, растровый замер `RasterRuling` и `TextStaysOnPaper` на положение линии `Y(x, U) + d`; отрицательный контроль — `perspective: null` на пресете линейки с перспективой. Проверка — `npm run test:stories` проходит, отрицательный контроль падает при подмене.
- [ ] 8.2 Переснять эталоны скриншотов (`npm run test:visual:update` в docker-образе) и сверить диф глазами: строки на линейке сдвинуты вдоль линий, а не переложены. Проверка — `npm run test:visual` проходит, итог сверки в журнале.
- [ ] 8.3 Обновить `CLAUDE.md`: «Как работает генератор» (контур, измерение внутри листа, перспектива в модели, раскладке, отрисовке и отражении, перемер границ) и «Ассеты» (поля `outline`/`perspective` в артефакте и записи своего листа, `measure:sheet`). Проверка — разделы описывают текущий код, `npm run lint` проходит.
- [ ] 8.4 Прогнать `npm run lint`, `npm run typecheck`, `npm test`. Проверка — 0 ошибок и 0 предупреждений в новом коде.
- [ ] 8.5 Приёмка скриптом на фото пользователя вне git: `npm run measure:sheet -- IMG_1602.jpg` и `-- IMG_1596.png`. Эталоны сняты вручную по снимкам при проектировании. Проверка — отчёт в журнале, и в нём:
  - **IMG_1602**:
    - разлиновка — клетка, шаг 47,9 ± 0,5 px;
    - найдены верхняя (41→79 px от верха ± 20 px) и правая (33 ± 20 px от края) стороны, левая и нижняя — край кадра;
    - поля сверху/справа/снизу/слева 140 / 223 / 123 / 133 ± ½ шага;
  - **IMG_1596**:
    - найдены все четыре стороны (слева 158→136, справа 145→102, сверху 43→136, снизу 59→0 px от краёв, ± 25 px);
    - клетка, перспектива записана, шаг у верхней линии 64,8 ± 1 px, у нижней 67,7 ± 1 px;
    - первая линия не ниже верхней линии сетки (y ≈ 160 у левого края сетки, ± ½ шага);
    - левое поле 330 ± ½ шага, нижнее 200 ± ½ шага;
    - правое поле — по крайней вертикали сетки (x ≈ 2625 вверху) плюс ⅕ шага;
  - `git status` показывает оба фото неотслеживаемыми, в коммиты они не попали.
- [ ] 8.6 Живая приёмка в браузере — пользователем: импорт IMG_1602.jpg и IMG_1596.png, страницы 1 и 2. Проверка:
  - текст не заходит на стол, обложку и спираль;
  - базовые линии на линиях клетки по всей странице, включая верхние строки IMG_1596;
  - чернила у края листа не темнее, чем в середине.

  Задача остаётся `[ ]` до подтверждения пользователя.
