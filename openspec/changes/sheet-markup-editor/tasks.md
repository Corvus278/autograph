## 1. Хранилище своих листов в IndexedDB

- [ ] 1.1 Обёртка IndexedDB без зависимостей в `model/` (`open`, `getAll`, `put`, `delete`, транзакция); `fake-indexeddb` — dev-зависимость, подключена в `tests/helpers/jsdom-setup.ts`. Проверка: юнит-тест записи и чтения Blob проходит в jsdom.
- [ ] 1.2 Новая форма записи своего листа (`kind`, `source: Blob`, `texture: Blob`, `measuredRuling`, `pins`, `ruling` с `mirrorOverride`) и чтение через `parsePaperSheet` с дефолтами для отсутствующих полей (вид — вид семьи, `mirrorOverride` — `null`, `pins` — выведенные). Проверка: тест «запись без новых полей читается» по каждому полю.
- [ ] 1.3 `userSheetsStorage` на IndexedDB: запись, удаление, асинхронное чтение при запуске; `src` листа — object URL, освобождается при удалении. Проверка: тест «лист переживает перезагрузку» — запись, новый экземпляр стора, чтение, разметка та же.
- [ ] 1.4 Перенос из `localStorage`: прежние ключи → Blob → одна транзакция → удаление ключей только после `complete`; идемпотентен. Проверка: тесты «перенос с ручной разметкой», «сбой транзакции оставляет `localStorage`», «повторный перенос ничего не дублирует».
- [ ] 1.5 Видимый отказ записи: `addUserSheet` и сохранение редактора читают результат; флаг в сторе; сообщение в `PaperPicker`. Проверка: компонентный тест с `put`, бросающим `QuotaExceededError`, показывает «Лист не сохранится после перезагрузки», лист доступен.
- [ ] 1.6 `navigator.storage.persist()` при первом сохранении своего листа. Проверка: юнит-тест — вызов ровно один раз.
- [ ] 1.7 Асинхронный старт: страница на своём листе показывает ожидание, пока листы не прочитаны; ссылки сессии проверяются после чтения. Проверка: компонентный тест восстановления сессии с закреплённым своим листом — фолбэк-бумага не мелькает, после чтения лист закреплён.

## 2. Модель разметки: булавки, изгиб, зеркало, вид

- [ ] 2.1 `SheetRuling.mirrorOverride` обязательным полем; `buildSheetRuling`, `mirrorSheetRuling` (применяет и обнуляет), `paperSheetJson`, пресеты — `null`; отпечаток кэша `measureLayout` включает поле. Проверка: `npm run typecheck`; тест «правка полей зеркала перекладывает правую страницу, левую нет»; `profiles.json` бит-в-бит без пересборки.
- [ ] 2.2 Подгонка гребёнки по булавкам в `lib/paper/` (решение 2): точное решение для двух, наименьшие квадраты для трёх и более, проверка годности знаменателя, невязка каждой булавки. Проверка: тесты на синтетике — две булавки ровного листа; три булавки листа со шагом −25 % к верху (промежуточные линии не дальше 1/20 шага); две непараллельные — все линии прямые и проходят через булавки ±1 px; вырожденная расстановка отвергнута.
- [ ] 2.3 Правка изгиба с затуханием (решение 3): добавок узлам по весам, масштаб по отклику `sampleBend`, радиусы, режим «весь столбец», создание сетки при `bend: null`. Проверка: тест — после правки линия проходит через точку ±1 px, линии за 2R не меняются больше чем на 0,5 px.
- [ ] 2.4 «Подогнать изгиб по фото» от текущей гребёнки с перспективой: если `detectRulingBend` уводит трассу на листе с перспективой — передать ему `perspective`. Проверка: тест на синтетике «перспектива + волна» — линии следуют волне не хуже 1/20 шага.
- [ ] 2.5 Вид у листа: `kind` в записи, семья по виду (решение 6), начальный вид из `RulingDetection.kind`. Проверка: тест — лист в клетку, импортированный при выбранной линейке, после сохранения в семье клетки.
- [ ] 2.6 Магнит линий: поиск минимума яркости в окне ±s/3 с уточнением параболой; магнит линии поля до группы 5 — тот же поиск по яркости. Проверка: юнит-тест на синтетике — отпущенный в 0,2 шага от линии конец встаёт на неё ±0,5 px; вдали от линий остаётся на месте.

## 3. Разбор снимка в воркере

- [ ] 3.1 `measureSheetPhoto` в dedicated Web Worker: `ImageBitmap` внутрь, измерение и производные наружу; импорт и перемер идут одним путём. Проверка: story/тест — во время разбора основной поток отвечает (таймер не задерживается больше 50 мс); результаты совпадают с разбором в основном потоке до бита на пресетах.
- [ ] 3.2 Сверить сценарии #18 и, если покрыты, добавить `Closes #18` в тело PR. Проверка: запись в `journal.md`.

## 4. Редактор разметки

- [ ] 4.1 Каркас полноэкранного диалога: фото по object URL, SVG-оверлей в пикселях фото, зум/панорама, слои, панель по решению 1; открывается после каждого импорта и с плитки листа; `SheetDialog`/`SheetDialogForm`/`manualRulingRequest` заменены. Проверка: компонентные тесты «редактор после импорта», «разлиновка не найдена — сообщение и две булавки», «повторное открытие показывает ручную разметку».
- [ ] 4.2 Линии в оверлее той же геометрией, что строки страницы (`getPageRuling`, `lineHeightAt`, `sampleBend`). Проверка: story-тест в Chromium — базовые линии пробного текста на линиях оверлея ±1 px на листе с перспективой и изгибом.
- [ ] 4.3 Инструмент «Контур»: четыре угла, проверка выпуклости и четверти кадра, перемер в воркере с ожиданием и блокировкой сохранения. Проверка: компонентные тесты «вывернутый контур не применён», «узкий контур — сообщение», «во время перемера сохранение недоступно».
- [ ] 4.4 Инструмент «Линии»: булавки, бейджи «N шагов» с ±, магнит с Alt, невязка булавки, подгонка изгиба по фото после правки. Проверка: компонентный тест «N с 10 на 20 → 19 промежуточных линий»; story-тест перетаскивания булавки.
- [ ] 4.5 Инструмент «Изгиб»: перетаскивание точки, кольцо и слайдер радиуса, Shift — весь столбец, «Подогнать по фото», «Выпрямить». Проверка: story-тест — после перетаскивания линия проходит через точку.
- [ ] 4.6 Инструмент «Поля»: верх/низ со встаёт-на-линию, бока, линия поля с «Убрать»/«Добавить» и магнитом, сообщения автомата. Проверка: компонентные тесты «ложная линия поля убрана → блок во всю ширину», «добавлена слева → блок правее».
- [ ] 4.7 Вид разлиновки в редакторе: смена вида, «Без разлиновки» без поиска линий, возврат к поиску. Проверка: компонентный тест «Без разлиновки → магнит не притягивает, строки через заданный шаг».
- [ ] 4.8 Точность: лупа при перетаскивании, стрелки на 1 px и на 10 px с модификатором, Tab по ручкам, `aria-label` у каждой ручки, «Точные значения» числами. Проверка: компонентный тест стрелки; story `a11y` без нарушений.
- [ ] 4.9 Пробный текст через воркер отрисовки по отпусканию с дебаунсом и отбрасыванием устаревших ответов. Проверка: компонентный тест — после отпускания уходит один запрос, устаревший ответ не показывается.
- [ ] 4.10 Вкладка зеркала: отражённое фото и разметка, правка через `x → W − x`, «Как у оригинала / Задать отдельно» для линии поля и боковых полей. Проверка: компонентные тесты «угол на зеркале меняет оригинал», «линия поля справа на обеих страницах», «возврат к отражению».
- [ ] 4.11 История редактора, «Сбросить к найденному» из `measuredRuling`, подтверждение закрытия с правками, «Сохранить» одним применением; хоткеи генератора не срабатывают при открытом редакторе. Проверка: компонентные тесты отмены, сброса, закрытия с подтверждением и доезда сохранения до страницы.
- [ ] 4.12 Stories и скриншотные эталоны редактора (фиксированный пресет, фиксированный seed) в docker-образе. Проверка: `npm run test:stories` и `npm run test:visual` зелёные.

## 5. После мержа #10: цветовой магнит и удержанная выборка

Группы 1–4 от этой не зависят и идут без неё. Удержанная выборка снимается после мержа #10, чтобы её гарантированно не видел ни один подбор порогов #10 (решение 9).

- [ ] 5.1 Ребейз на `master` с вмерженным #10. Проверка: `git log` содержит коммит #10; `npm test` зелёный; воркер разбора (3.1) отдаёт канал `R − G`, и числа на пресетах совпадают с разбором в основном потоке до бита.
- [ ] 5.2 Проверить, что эталон цвета перенесён #10 в репозиторий (`npm run measure:margin-color`). Если нет — перенести из `/tmp/false-margin-line/` в `scripts/margin-line-ground-truth.ts` тем же путём сборки, что `measure:sheet`. Проверка: на `IMG_1813` печатает черту left, самое внутреннее `u` ≈ 401, ΔR 41,0 против ΔG 60,8; на `IMG_1803` столбец 111,7 — `R < G`.
- [ ] 5.3 Цветовой магнит линии поля в редакторе (решение 7): максимум избытка `R − G` в окне ±s/3, на сером снимке — яркостный. Проверка: юнит-тест на синтетике с цветом из #10 — отпущенная в 0,2 шага от красной черты вертикаль встаёт на черту ±0,5 px, а не на соседнюю вертикаль клетки; компонентный тест «добавленная линия поля притянута к черте».
- [ ] 5.4 Попросить пользователя снять 15–20 новых листов (разные тетради, свет, поверхности; с линией поля и без) и положить их в папку вне репозитория, подключённую симлинком `/paper_photos_holdout`; добавить симлинк в `.gitignore`. Проверка: `git status` симлинк не показывает, `ls paper_photos_holdout | wc -l` — от 15 до 20.
- [ ] 5.5 Снять `sha256` всех файлов выборки в `openspec/changes/sheet-markup-editor/holdout.sha256` и закоммитить. Файлы не открывать ни одним прогоном до группы 7. Проверка: `shasum -a 256 -c holdout.sha256` проходит.

## 6. База знаний

- [x] 6.1 `docs/notes/README.md` — назначение (отложенные направления и исследования, не читаются агентом по умолчанию) и индекс; `docs/notes/ruling-detection-directions.md` — робастная подгонка одной модели (LSD/Hough + RANSAC), ML-сегментация, датасеты (DocDewarpHV, DocUNet, DIR300, WarpDoc — почему не подходят), размер выборки (правило трёх, 200–500 + 50–100), запуск модели в браузере (ONNX Runtime Web, WebGPU/WASM, квота precache 2 МиБ), гибрид «карта вероятности + своя геометрия», источник данных — правки в редакторе. Проверка: файлы существуют, ссылки на источники в заметке.
- [x] 6.2 Строка-указатель на `docs/notes/` в `CLAUDE.md` (раздел «Структура») и в заметке — пометка о нецветных линиях поля (решение 7, риски). Проверка: `rg -n "docs/notes" CLAUDE.md` — одна строка.

## 7. Приёмка

- [ ] 7.1 Открыть удержанную выборку: проверить `sha256`, разметить цветом (`measure:margin-color`), прогнать `measure:sheet` по каждому снимку один раз. Проверка: таблица «снимок → цвет, детектор» в `journal.md` и в теле PR; число линий поля там, где цвет её не видит, и доля найденных настоящих — числами. Провал — новый issue на детектор, не подкрутка порогов в этом PR.
- [ ] 7.2 Живая проверка в браузере (`npm run dev`): импорт `IMG_1813`, удаление фантома и добавление черты слева в редакторе, лист со стола с тремя булавками, прогиб у пружины, зеркало с линией поля на той же стороне, перезапуск браузера, пять снимков по 4 МБ. Проверка: запись в `journal.md` со скриншотами.
- [ ] 7.3 `CLAUDE.md`: разделы «Структура», «Как работает генератор» (импорт → редактор, булавки, изгиб, зеркало), «Ассеты» (хранилище IndexedDB, перенос), «Тесты». Проверка: разделы описывают текущий код, устаревших упоминаний `SheetDialogForm` и `localStorage` для листов нет (`rg`).
- [ ] 7.4 `npm run lint`, `npm test`, `npm run test:pwa`, `npm run test:stories`, `npm run test:visual`; версия — минор (`npm version <минор> --no-git-tag-version`). Проверка: все зелёные, `package.json` и `package-lock.json` с новой версией.
- [ ] 7.5 Время разбора снимка до и после на 26 живых снимках. Проверка: рост больше 20 % или больше 3 с на снимок — числом в тело PR.
