## 1. Опора для замеров и удержанная выборка

- [x] 1.1 Убедиться, что работа идёт в ветке `fix/9-margin-line-trace` от свежего `master`. Проверка: `git log --oneline -1 master..HEAD` показывает только коммиты этой задачи.
- [x] 1.2 Снять исходные числа по восемнадцати видимым снимкам `paper_photos_examples/` (все 26, кроме восьми удержанных из 1.3) и по восьми пресетам: `npm run measure:sheet` по каждому, вывод в файл вне репозитория. Проверка: у `IMG_1806` и `IMG_1807` в файле стоит «линия поля нет», у пресетов строка линии поля совпадает с `marginLineX` из `public/paper/profiles.json`.
- [x] 1.3 Отложить удержанную выборку: `IMG_1602`, `IMG_1602 2.jpg`, `IMG_1702`, `IMG_1707`, `IMG_1709`, `IMG_1803`, `IMG_1810`, `IMG_1812` — восемь снимков, среди них второй файл того же листа, что `IMG_1602`, и отрицательный класс `IMG_1812` (линии поля нет), без которого выборка не ловит подгонку множителя под видимые негативы (решение 7 в `design.md`). Прогнать `npm run measure:sheet` по ним **с перенаправлением в отдельный файл вне репозитория, не открывая файл**, и не возвращаться к нему до задачи 7.3. Проверка: файл существует, в журнале прогона записаны имена восьми снимков без чисел.
- [x] 1.4 Снять ground truth для `IMG_1806` и `IMG_1807` **скриптом**, а не глазом. Во временном скрипте вне репозитория декодировать снимок тем же декодером, что `scripts/sheet-photo-report.ts`, и напечатать для трёх горизонтальных полос (верх, середина, низ области с линиями) `x` самого глубокого столбца в крайней трети со стороны линии поля плюс шаг разлиновки из отчёта; ground truth — самое внутреннее из трёх `x`. Проверка: скрипт печатает по три числа на снимок, у `IMG_1806` самое внутреннее сходится с числом из issue (≈ 2494 px) в пределах пяти пикселей, разброс трёх полос не больше шага, числа лежат в файле замеров и служат эталоном задач 6.1 и 6.2. **Требует человека:** один раз открыть обе фотографии и подтвердить, что скрипт указал на красную черту, а не на вертикаль клетки; без подтверждения задача не закрывается.
- [x] 1.5 Зафиксировать опору неизменности артефакта. Проверка: `md5 -q public/paper/profiles.json` печатает `bbb9e9b963bc092e50aad90f60fd39ce`.

## 2. Синтетические листы в хелперах тестов

- [x] 2.1 Расширить генератор синтетических листов в `tests/helpers/synthetic-sheet.ts`: линия поля сносится по `x` на заданную долю шага от верха к низу области с линиями (снос + уже имеющийся изгиб). Проверка: юнит-тест с нулевым сносом даёт растр пиксель в пиксель прежний, а с ненулевым — столбец линии в верхней и нижней полосе отличается на заданное число пикселей.
- [x] 2.2 Добавить лист в клетку **без** линии поля с тем же зерном и контрастом вертикалей — отрицательный класс калибровки. Проверка: юнит-тест утверждает, что глубины всех вертикалей на нём различаются не больше чем на четверть.
- [x] 2.3 Добавить лист, где линия поля пропадает в нескольких полосах подряд (пятно поверх неё). Проверка: юнит-тест меряет, что в этих полосах глубина линии ниже десятой доли типичной.
- [x] 2.4 Добавить лист в клетку без линии поля, у которого самая глубокая вертикаль стоит **ровно на фазе** вертикальной гребёнки, — опора различителя задачи 3.7. Проверка: юнит-тест утверждает, что `x` самой глубокой вертикали отстоит от фазы гребёнки меньше чем на шестую долю шага.

## 3. Полосовая ступень поиска линии поля

- [ ] 3.1 Написать в `src/pages/Generator/lib/paper/detectRuling.ts` полосовой опрос: профиль столбцов и снятие фона в каждой полосе, кандидаты из крайних третей самой глубокой полосы и двух соседних, трасса кандидата по всем полосам, оценка медианой глубины и долей найденных полос (решение 2 из `design.md`). Проверка: юнит-тест на листе из 2.1 со сносом в полтора шага — кандидат найден, его медианная глубина не меньше половины глубины линии в одной полосе.
- [ ] 3.2 Считать барьер по полосам с обеих сторон сравнения: дециль глубин вертикалей средней трети меряется **тем же** полосовым способом, что и кандидат (решение 3). Множитель на этом шаге остаётся нынешним `MARGIN_LINE_DEPTH_RATIO = 1,5` — собственной константы ступень не получает до замера 3.4. Проверка: юнит-тест на листе из 2.2 — линия поля не найдена; на листе из 2.1 — найдена; в обоих случаях тест читает из диагностики отношение «кандидат / дециль соседей» и имя связавшего порога.
- [ ] 3.3 Включить ступень в `detectRuling` второй: только когда `findMarginLine` по профилю во всю высоту вернула `null`, и за тем же гейтом `marginLineSide` (решения 1 и 5). Проверка: юнит-тест на синтетическом ровном листе с прямой линией поля фиксирует, что полосовой путь не вызывался и `marginLineX` совпал с прежним до бита; юнит-тест с `marginLineSide: null` — линия не ищется ни одной ступенью.
- [ ] 3.4 Померить полосовым способом **обе** стороны сравнения на всех восемнадцати видимых снимках и решить по замеру судьбу множителя (решение 3). Проверка: в файле замеров лежит таблица «снимок → глубина кандидата, дециль соседей, их отношение, связавший порог (`соседи` / `сигма` / `минимум`), класс (линия есть / линии нет)», и по ней записан вывод одного из трёх видов: (а) нынешний 1,5 разделяет классы с запасом не меньше 0,1 в обе стороны — новой константы не заводится, и это записано явно; (б) не разделяет, но зазор есть — новая константа ставится строго между наибольшим отношением у негативов и наименьшим у позитивов, с запасом не меньше 0,1 до обоих; (в) зазора нет — множитель не трогается, открывается задача 3.7. Отдельным числом в той же таблице: у `IMG_1596` барьер обязан быть не меньше чем в **1,5 раза** выше глубины лучшего кандидата — запас задачи #8 в 1,6 % приёмку не проходит.
- [ ] 3.5 Довести отказ: линия не заводится на листе без неё и на листе, где трасса нашла её меньше чем в пороге полос. Проверка: юнит-тесты на листах из 2.2 и 2.3 — `marginLineX` равен `null` у первого и найден у второго (пятно полосы не отменяет линию).
- [ ] 3.6 Обеспечить повторяемость: два измерения одной фотографии дают одинаковый результат. Проверка: юнит-тест сверяет измерение через `toStrictEqual`, как сторож повторного импорта из задачи #8.
- [ ] 3.7 Закрыть ветку «зазора нет» решения 4. Если вывод 3.4 — вариант (в), ввести различитель «одиночка против гребёнки»: кандидат, стоящий на фазе вертикальной гребёнки клетки с точностью до `LINE_SEARCH_SHARE` шага, берёт повышенный барьер, кандидат вне фазы — обычный. Проверка при варианте (в): юнит-тест на листе из 2.4 — линия не найдена; на листе из 2.1 — найдена; пересчитанная с различителем таблица 3.4 разделяет классы с запасом не меньше 0,1. Проверка при вариантах (а) и (б): код не трогается, а в файле замеров записано «различитель не потребовался, зазор <число>» — задача закрывается этой записью.

## 4. Диагностика и отчёт замера

- [ ] 4.1 Провести в `SheetPhotoDiagnostics` ступень, которой найдена линия поля (`профиль` / `полосы` / не найдена), долю полос с найденным узлом, отношение глубины к децили соседей и **имя связавшего порога** (`соседи` / `сигма` / `минимум`). Проверка: юнит-тест на ровном листе и на листе из 2.1 читает ожидаемые значения, а на листе из 2.2 — что кандидат отвергнут и назван порог, который его связал.
- [ ] 4.2 Печатать эти числа в строке «линия поля» в `scripts/sheet-photo-report.ts` (решение 6). Проверка: `npm run measure:sheet -- paper_photos_examples/IMG_1806.jpeg grid` печатает строку со ступенью, охватом, отношением и связавшим порогом.

## 5. Пресет-пак остаётся бит-в-бит

- [ ] 5.1 Прогнать восемь пресетов сразу после 3.3, до калибровки 3.4. Проверка: строка линии поля у каждого совпадает с числами из 1.2, ступень у всех восьми — `профиль`.
- [ ] 5.2 Прогнать `npm run build:paper`. Проверка: `git diff --stat public/paper/profiles.json` пуст и `md5 -q public/paper/profiles.json` печатает `bbb9e9b963bc092e50aad90f60fd39ce`. Расхождение — правка задела работающий путь и возвращается на доработку.

## 6. Приёмка по предмету issue

- [ ] 6.1 Прогнать `npm run measure:sheet` по `IMG_1806` и `IMG_1807`. Проверка: у обоих линия поля найдена и отстоит от ground truth задачи 1.4 не больше чем на пятую долю шага.
- [ ] 6.2 Проверить, что блок не заходит за черту, **юнит-тестом**, а не импортом через интерфейс: записать разлиновку обоих снимков из отчёта 6.1 фикстурами в тест и посчитать по ним `deriveGeometry`. Проверка: правый край блока отстоит от `marginLineX` внутрь не меньше чем на пятую долю шага на всей высоте страницы; у `IMG_1806` край уходит с 2739 px внутрь черты.
- [ ] 6.3 Проверить зеркальную страницу тем же тестом поверх `mirrorSheetRuling`. Проверка: линия поля оказалась у противоположной стороны листа, край блока не заходит за неё на той же пятой доле шага.
- [ ] 6.4 Проверить попадание текста на линии на этих листах той же проверкой, что в `BaselineFit.stories.tsx`. Проверка: отклонение базовых линий не больше десятой доли шага — линия поля не должна была сдвинуть разлиновку.
- [ ] 6.5 Сверить сетку изгиба на снимках, где линия поля появилась **впервые** (`IMG_1806`, `IMG_1807`): найденная линия обрезает область изгиба, и узлы сетки переезжают (решение 5). Проверка: в отчёте 6.1 граница области изгиба со стороны линии поля совпала с `marginLineX` в пределах одной полосы выборки, доля найденных узлов не упала ниже 60 % (порог проверки надёжности изгиба), наибольший отход изгиба остался выше двадцатой доли шага — то есть изгиб не потерялся от сужения области, — а проверка 6.4 на этих же листах прошла на **пересчитанной** сетке.

## 7. Неухудшение и удержанная выборка

- [ ] 7.1 Прогнать отчёт по восемнадцати видимым снимкам и сверить с 1.2. Проверка: ни один снимок не потерял найденную линию поля и не сдвинул её больше чем на пятую долю шага; шаг, фаза и угол не изменились.
- [ ] 7.2 Проверить фантомы поимённо на видимых снимках. Проверка: у `IMG_1596` линии поля нет **и** барьер стоит не меньше чем в 1,5 раза выше глубины лучшего кандидата (число из таблицы 3.4); у `IMG_1813` строка линии поля совпала с 1.2 до последнего знака. `IMG_1812` вскрывается вместе с выборкой в 7.3.
- [ ] 7.3 Вскрыть удержанную выборку из 1.3 и прогнать по ней отчёт **один раз**, без правок после прогона. Проверка: ни один из восьми снимков не потерял найденную ранее линию поля и не приобрёл новую без подтверждения по фотографии; у `IMG_1812` линии поля по-прежнему нет (появилась — значит множитель подогнан под видимые негативы); `IMG_1602` и `IMG_1602 2.jpg` дали согласованный ответ — линия есть у обоих или нет ни у одного, и положение сходится в пределах пятой доли шага с поправкой на кадр; шаг не изменился больше чем на 2 %. Расхождение — повод вернуться к решениям, а не подкрутить множитель.
- [ ] 7.4 Сверить время измерения по **медиане трёх прогонов** на снимок: одиночный прогон здесь ничего не доказывает — журнал задачи #8 фиксирует разброс самого замера до 18 %. Проверка: у снимков, где линия поля находится первой ступенью, медиана выросла не больше чем на 20 % против медианы трёх прогонов до правки; у `IMG_1806` и `IMG_1807` абсолютный рост не больше 5 с на снимок.

## 8. Гейт и задача целиком

- [ ] 8.1 Прогнать `npm run lint` и `npm test`. Проверка: 0 errors и 0 warnings, все тесты зелёные.
- [ ] 8.2 Прогнать `npm run test:stories`. Проверка: попадание базовых линий на разлиновку и проверки `a11y` зелёные.
- [ ] 8.3 Открыть PR с телом `Closes #9`, приложив числа из 3.4, 3.7, 5.2, 6.1—6.5 и 7.1—7.4. Проверка: `gh pr view` показывает связанный issue.
- [ ] 8.4 Провести аудит ветки против `master` (скилл `review-staged`), разложив находки inline-комментами. Проверка: треды заведены на строках диффа.
- [ ] 8.5 Поправить находки и резолвить треды, подтверждённые кодом; повторить аудит до пустого списка. Проверка: GraphQL-запрос нерезолвнутых тредов возвращает пусто.
