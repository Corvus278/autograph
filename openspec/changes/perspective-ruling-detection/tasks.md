## 1. Опора для замеров и удержанная выборка

- [ ] 1.1 Завести issue-ветку от свежего `master` (`fix/8-perspective-ruling-detection`) и убедиться `git log --oneline -1`, что в ней только коммит `master`.
- [x] 1.2 Снять исходные числа: прогнать `npm run measure:sheet` по десяти отказным снимкам (`IMG_1705`, `IMG_1800`—`IMG_1802`, `IMG_1804`, `IMG_1805`, `IMG_1808`—`IMG_1811`) и по восьми пресетам пресет-пака, сложить вывод в файл вне репозитория. Проверка: у десяти шаг `0.00 px`, у пресетов — числа из `public/paper/profiles.json`.
- [x] 1.3 Отложить удержанную выборку: выбрать из `paper_photos_examples/` четыре снимка (`IMG_1596`, `IMG_1703`, `IMG_1806`, `IMG_1813`) и не смотреть на них до задачи 7.3. Проверка: их нынешние числа записаны в тот же файл и до 7.3 не перечитываются.
- [x] 1.4 Зафиксировать время измерения по снимкам из 1.2 — точка сравнения для задачи 7.4.

## 2. Синтетические листы в хелперах тестов

- [x] 2.1 Расширить генератор синтетических листов в `tests/helpers/`: шаг растёт по высоте на заданную долю (схождение по высоте), линии сходятся по ширине на заданную долю. Проверка: юнит-тест, рисующий лист с нулевыми параметрами, даёт пиксель в пиксель прежнюю картинку.
- [x] 2.2 Добавить лист, у которого в части кадра различима лишь каждая вторая линия (клетка «через одну»). Проверка: юнит-тест утверждает, что в этой части период вдвое больше, чем в остальной.
- [x] 2.3 Добавить две ловушки: лист без линий с полосой периодического текста в середине кадра и лист, где линии есть только в половине кадра. Проверка: юнит-тест на каждой — период либо не находится, либо не выстраивается в монотонный ряд по высоте.

## 3. Полосовой замер периода

- [x] 3.1 Написать `src/pages/Generator/lib/paper/measureBandedPeriod.ts`: нарезка полос поперёк линий, свой свип и период в каждой, сведение кратных шагов к одному семейству, подгонка `step(u)` и отдача шага в середине, фазы, наклона и засева схождения (решения 1—3 из `design.md`). Проверка: юнит-тест на синтетическом листе из 2.1 с дрейфом 25 % возвращает шаг с точностью 2 % от шага в середине листа и засев схождения с точностью 10 %.
- [x] 3.2 Довести сведение кратных: на листе из 2.2 итоговый шаг равен шагу линий, а не удвоенному. Проверка: юнит-тест, оба направления (кратный шаг в верхних полосах и в нижних).
- [x] 3.3 Довести отказ: на обеих ловушках из 2.3 и на листе без линий шаг — `0`. Проверка: юнит-тесты на каждый случай.
- [x] 3.4 Вывести число полос из грубой оценки шага, с нижней границей в две соседние полосы. Проверка: юнит-тест на листе с шагом в шестую долю высоты кадра — замер не разваливается; на листе, где и двух полос не набирается, — честный `0`.
- [x] 3.5 Обеспечить повторяемость: два прогона по одной выжимке дают одинаковый шаг до бита. Проверка: юнит-тест.

## 4. Полосовая ступень в детекторе разлиновки

- [x] 4.1 Включить `measureBandedPeriod` в `detectRuling` второй ступенью — только когда глобальный профиль не взял `RULING_CONFIDENCE_THRESHOLD`, и в проходе по кадру, и в проходе по выпрямленной копии (решение 4). Проверка: юнит-тест на ровном синтетическом листе фиксирует, что полосовой путь не вызывался и числа совпадают с прежними до бита, а на листе с перспективой — что разлиновка найдена.
- [x] 4.2 Провести засев схождения из `detectRuling` в `detectRulingPerspective` через `measureSheetPhoto`. Проверка: юнит-тест на синтетическом листе с дрейфом 25 % — перспектива найдена, а без засева тот же лист её теряет.
- [x] 4.3 Провести шаги согласившихся полос и дрейф засева в `SheetPhotoDiagnostics`. Проверка: юнит-тест на ровном листе (полосы не понадобились) и на листе с перспективой читает ожидаемые числа.
- [x] 4.4 Печатать числа полос в `scripts/sheet-photo-report.ts`. Проверка: `npm run measure:sheet -- paper_photos_examples/IMG_1808.jpeg grid` печатает строку с шагами полос и дрейфом.

## 5. Порог дрейфа шага в детекторе перспективы

- [x] 5.1 Поднять `MAX_STEP_DRIFT` в `detectRulingPerspective.ts` до 0,35, переписав jsdoc под роль порога из решения 5. Проверка: юнит-тест на синтетике — лист с дрейфом 25 % сохраняет перспективу, лист с дрейфом 60 % её теряет.
- [x] 5.2 Убедиться, что остальные проверки надёжности не ослабли: лист с оборванной на половине кадра трассой по-прежнему отдаёт лист без перспективы, и лист, где линии не согласуются ни с одной перспективой, — тоже. Проверка: два юнит-теста.
- [x] 5.3 Проверить сценарий «Шаг меняется сильнее гарантии»: на синтетике с двукратным изменением шага лист либо сохраняется без перспективы, либо восстанавливает линии с точностью двадцатой доли шага. Проверка: юнит-тест.

## 6. Пресет-пак — до правок и после

- [x] 6.1 Прогнать восемь пресетов через `detectRuling` сразу после задачи 4.1, до остальных правок. Проверка: шаг, фаза, угол и уверенность каждого совпадают с числами из 1.2.
- [ ] 6.2 Прогнать `npm run build:paper`. Проверка: `git diff --stat public/paper/profiles.json` пуст; иначе полосовая ступень задела работающий путь и правка возвращается на доработку.

## 7. Приёмка по фотографиям

- [ ] 7.1 Прогнать `npm run measure:sheet` по десяти снимкам из 1.2. Проверка: у каждого разлиновка найдена, перспектива измерена, а базовые линии на листе ложатся на разлиновку — критерий попадания, а не совпадение с реперным числом (см. 7.2).
- [ ] 7.2 Проверить попадание текста на линии: в Storybook на листах, импортированных из `IMG_1808`, `IMG_1804` и `IMG_1705`, отклонение базовых линий не больше десятой доли шага — той же проверкой, что в `BaselineFit.stories.tsx`. Проверка: наблюдаемо в `npm run storybook`, числа — в теле PR.
- [ ] 7.3 Прогнать удержанную выборку из 1.3 **один раз**, без правок после прогона. Проверка: ни один из четырёх снимков не потерял найденный ранее шаг и не изменил его больше чем на 2 %. Расхождение — повод вернуться к решениям, а не подкрутить порог.
- [ ] 7.4 Прогнать отчёт по всем 26 снимкам и сверить с прошлым прогоном и с временем из 1.4. Проверка: ни один снимок с найденным шагом его не потерял; время у снимков, которые измерялись и раньше, не выросло.
- [ ] 7.5 Повторить импорт одного и того же файла дважды и сверить шаг. Проверка: числа совпадают (требование «Повторный импорт даёт те же числа»).

## 8. Гейт и задача целиком

- [ ] 8.1 Прогнать `npm run lint` и `npm test`. Проверка: 0 errors и 0 warnings, все тесты зелёные.
- [ ] 8.2 Прогнать `npm run test:stories`. Проверка: попадание базовых линий на разлиновку и проверки `a11y` зелёные.
- [ ] 8.3 Открыть PR с телом `Closes #8`, приложив числа из 7.1—7.4. Проверка: `gh pr view` показывает связанный issue.
- [ ] 8.4 Провести аудит ветки против `master` (скилл `review-staged`), разложив находки inline-комментами. Проверка: треды заведены на строках диффа.
- [ ] 8.5 Поправить находки и резолвить треды, подтверждённые кодом; повторить аудит до пустого списка. Проверка: GraphQL-запрос нерезолвнутых тредов возвращает пусто.
