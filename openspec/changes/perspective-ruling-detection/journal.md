# Журнал прогона

Решения без заказчика, отступления от спеки, итоги аудитов и долг прогона change `perspective-ruling-detection`.

## 2026-09-22

### Старт прогона

База прогона: d7a3243e41311eb6b7609472eaf93791081b7eb5 (ветка fix/8-local-ruling-step, issue #8).
Масштаб: средний, 31 задача, 8 групп по tasks.md.
Гейт: npm run lint && npm test (CLAUDE.md, «Линтинг»); npm run build:paper — только в группе пресет-пака.
Живые образцы: /Users/corvus278/Projects/autograph/paper_photos_examples/ (симлинк, 26 фото).
Предмет приёмки issue #8: IMG_1705, IMG_1800, IMG_1801, IMG_1802, IMG_1804, IMG_1805, IMG_1808, IMG_1809, IMG_1810, IMG_1811.
Удержанная выборка (проверяется один раз в конце) — по задачам 1.3/1.4 и группе 7.

### План принят

plan.md: 8 групп, 6 волн, дерево общее — исполнители по одному.
Вне групп: 1.1 (ветка уже заведена, база d7a3243 на fix/8-local-ruling-step); 7.2 (живая story с фото не коммитится — фото вне репозитория; числа отклонения закрыты усилением 7.1 арифметикой по tests/helpers/baseline-model.ts); 8.3—8.5 (PR, review-staged, резолв тредов — шаг воркфлоу после прогона, делает координатор).
Контракты стыков K1—K6 — в plan.md.
Риск на контроле: подъём MAX_STEP_DRIFT до 0,35 может дать перспективу пресетам с perspective: null и изменить profiles.json вопреки Migration Plan. Проверка build:paper стоит сразу за 5.1 в G5; расхождение — вопросом к пользователю, а не новыми числами в артефакте.

### G1 · Числа до правок и удержанная выборка

Аудит G1: verdict ok, кругов 0. Продукт группы — вне репозитория: /tmp/perspective-ruling-detection/ (baseline.md, holdout.md, сырые выводы, скрипты сверки).
Опора: 10 отказных снимков предмета приёмки (у всех шаг 0.00 px), 8 пресетов сверены с profiles.json скриптом по 12 полям, время — три прогона на файл (разброс замера до 18 %, поэтому сверка в 7.4 идёт по медиане).
Решение координатора по долгу аудита: 6.1 сужается до полей, которые печатает scripts/sheet-photo-report.ts (confidence он не печатает); «уверенность» из текста 6.1 проверяется юнит-уровнем в G4, а не отчётом. Альтернатива — учить отчёт печатать confidence — расширила бы диагностику вне Impact proposal.
Долг: 7.4 обязана мерить время тем же способом, что 1.4 (бандл node_modules/.cache/measure-sheet.mjs напрямую), иначе медианы несопоставимы.
K5 соблюдён: в baseline.md имена удержанных снимков есть, чисел нет.

### G2 · Синтетика с дрейфом шага

Аудит G2: verdict ok, кругов 0.
Хелпер получил доли stepDrift и lineConvergence, переводимые в существующую SyntheticRulingPerspective: доли поверх модели отброшены, иначе две геометрии разошлись бы и computeSyntheticLineY перестал быть эталоном.
Дрейф отсчитывается между крайними нарисованными линиями — так его меряет detectRulingPerspective; отсчёт по краям области давал 23,4 % вместо заказанных 25 %.
K4 проверен аудитором независимо: эталоны растра сняты на базе прогона и совпали на всех четырёх ветвях отрисовки; зонд по дрейфу 0,25—1,0 показал, что знаменатель 1 − a·q в углах не ниже 0,818, то есть тест G5 «дрейф 60 % теряет перспективу» упрётся в MAX_STEP_DRIFT, а не в чужой гард.
Подозрение на потерю прежних тестов снято механически: один хвостовой ханк, нуль удалённых строк, 36 прежних проверок зелёные.
Долг (в круг не берём): два стилевых пункта в tests/synthetic-sheet.test.ts (цепочка вместо reduce в measureRowGaps, нетипизированный литерал пар) и предупреждение для G3/G5 — computeEdgeLines в тесте считает крайние линии без учёта margins и совпадает с перспективой только на листе с нулевыми полями.

### Решения по G3

Порядок задач внутри G3 изменён: 3.4 (число полос из грубой оценки шага) идёт перед 3.3 (отказ на ловушках). Причина из отчёта исполнителя: ловушка HALF_RULED_SHEET при жёстких четырёх полосах даёт ту же картину, что IMG_1705 (две соседние найденные полосы), и правило «две соседние» приняло бы её вопреки 3.3. При выводе полос из шага кадр ловушки режется на две полосы, найдена одна — отказ выходит сам, а IMG_1705 на своих ~9 полосах остаётся найденным. Альтернатива — особое правило под ловушку — подгоняла бы детектор под тест.
Контракт K1 расширен шестым полем: засев отдаёт начало отсчёта координаты вдоль линий вместе со схождением. Без него G6 переносит convergence от середины области с линиями на середину кадра и ошибается на k·Δ (до ~10 % на снимках предмета приёмки). Приоритет надёжности над простотой: засев с известным origin переводится точно.
Замер на живых снимках после 3.1—3.2: у всех десяти отказных шаг не 0 (в опоре был 0.00), ряд шагов полос монотонный, дрейф 13…22 % (IMG_1705 — 60 % до сведения кратных), 106…139 мс на снимок против 130 мс глобального замера.

### G3 · Полосовой замер периода

Аудит G3: verdict ok, кругов 0, плюс два вопроса о границе приёмки (ниже).
Десять снимков предмета приёмки отдают ненулевой шаг (в опоре 0.00): 60.13 · 135.76 · 128.90 · 133.19 · 124.82 · 120.33 · 84.85 · 86.16 · 91.06 · 88.15. Цена 127…259 мс против 130 мс глобального замера; дизайн закладывал 0,2 с.
Решения исполнителей: число полос выводится из грубой оценки шага и ручного перебива не имеет; запасная оценка по кадру целиком включается там, где разбивка по умолчанию отказывает (на крупной разлиновке); порог точности на крупной разлиновке в пикселях, а не в долях шага; введён гард поддержки гребёнки MIN_COMB_SUPPORT_SHARE = 0,3 сверх решения 2 design.md — без него ловушку с полосой текста не отсекает ни одно выводимое число полос. Аудитор перебрал порог: рабочее окно 0,15…0,45, под один тест не подогнан.
Долг: K1 в plan.md перечисляет пять полей, модуль отдаёт шесть (origin) — запись контракта поправлена координатором; наблюдения за skewAngle на полосовом пути в группе нет — переносится в G6; цепочка вместо reduce в tests/paper-banded-period.test.ts:246.
Вопросы аудита (граница приёмки, решаются отдельной группой F1 после замера диагностом): 1) полоса печатного текста, занявшая две полосы целиком, проходит гард в кадре 480×1200 (step 19.92) — сколько шагов постороннего периода подряд ещё не разлиновка; 2) лист с линиями в половине кадра отсекается разбивкой только при шаге 20 на высоте 400, при шаге 10—12 период находится.

### Граница приёмки периодичности — решение по замеру диагноста

Вопросы аудита G3 (полоса текста проходит гард; половинный лист отсекается только разбивкой) решены без пользователя: он разрешил решать самому, а выбор сделан по числам, а не на глаз.
Замер диагноста на десяти снимках приёмки и на ловушках с разными шагами и кадрами: доля найденных полос снимки 0,50…1,00 против ловушек 0,25…0,60 — диапазоны перекрываются; охват в шагах периода (снимки 14,8…52,2, ловушки 14,6…43,8) не разделяет вообще; «касание середины» и «выход к краям» теряют IMG_1800.
Выбрано правило: охват гребёнки по кадру. После подгонки итоговый шаг step(u) промеряется при лучшей фазе в каждой полосе кадра, включая непринятые, и требуется, чтобы доля полос, держащих не меньше 15 % медианного контраста найденных, была не ниже 0,7. Числа: у всех десяти снимков и у полезной синтетики доля 1,00, у ловушек 0,25…0,60 — запас двусторонний и не съедается порогом относительного контраста (в окне 0,10…0,30 снимки 0,75…1,00, ловушки не выше 0,60).
Почему это, а не подсчёт полос: правило продолжает логику уже стоящего MIN_COMB_SUPPORT_SHARE — «настоящая разлиновка идёт через всю полосу» распространяется с полосы на кадр; клочок постороннего периода вне своего клочка даёт 0…0,21, а непринятые полосы живых снимков держат 0,18…1,95 (линии там есть, не хватило уверенности). Отвергнутые варианты: доля найденных полос ≥ 0,7 (теряет IMG_1800, IMG_1805, задевает IMG_1802 и IMG_1804), минимум 3 полосы (теряет IMG_1800 и IMG_1805), охват в шагах и геометрические правила (не разделяют).
Реализация — группой F1 перед G4, чтобы встраивание в detectRuling шло уже с итоговым правилом. Цена — один лишний профиль на непринятую полосу, тот же порядок, что у идущего замера.

### G5 · Порог дрейфа и надёжность перспективы

Аудит G5: verdict ok, кругов 0.
K6 проверен аудитором своими руками: npm run build:paper оставляет public/paper/profiles.json бит-в-бит прежним, все восемь пресетов без перспективы — отбраковка по расхождению гребёнок, а не по порогу. Migration Plan спеки не нарушен.
Порог держится тестами с обеих сторон (мутации 0,15 и 0,30 роняют ровно листы 25 % и 34 %), тест отказа на 60 % пришит именно к порогу, тесты 5.2 — к невязке.
Побочная находка исполнителя (foundNodeShare ровно 0,6 у листа с закрашенной половиной кадра) разобрана аудитором и дефектом не признана: включающая граница оставляет лист, который модель описывает с ошибкой 0,069 px при допуске 1,2 px. Гард чужой, группой не трогался.
Долг: jsdoc у листа с дрейфом 60 % объясняет причину отказа неверно (модель на нём строится, отбраковывает порог); в отгружаемой конфигурации вторая ветка дизъюнкции теста 5.3 не исполняется — зубы держат drift > 0,35 и step > 0.

### F1

Аудит F1: verdict ok, кругов 0, но с вопросом, который уходит в F2 (ниже).
Закрыты обе дыры аудита G3: полоса печатного текста, занявшая полосы разбивки целиком, и половинный лист при шагах 10 и 12 отдают шаг 0. Зубы проверены аудитором: снятие гарда роняет ровно три новых кейса, прочие 13 держатся прежними проверками. Пороги перебраны — охват 0,7 в окне 0,61…0,75, под один тест не подогнан. K6 держится: profiles.json бит-в-бит (md5 сверен до и после build:paper).
Вопрос аудита, принятый координатором к доработке: пороги калиброваны на кадре целиком, а detectRuling получает вырезку по контуру (measureSheetPhoto.ts:633) — именно туда задача 4.1 встраивает полосовую ступень. На вырезке IMG_1805 даёт охват 0,50 и теряется (шаг 0 против 120.93 до F1), IMG_1804 — 0,75 впритык. Порог относительного контраста 0,15 стоит на верхнем краю живого окна: при 0,16 IMG_1805 падает уже на кадре целиком.
Решение: границу калибруем заново на том изображении, которое детектор реально получает (вырезка по контуру), замером диагноста по всем десяти снимкам и ловушкам, и правим пороги группой F2 до встраивания в G4. Иначе G4 потеряет IMG_1805 — снимок предмета приёмки issue #8.
Долг: jsdoc MIN_BAND_CONTRAST_SHARE и запись журнала о «двустороннем запасе» не соответствуют замеру (IMG_1805 = 0,50 при пороге 0,16…0,45) — поправить вместе с порогами в F2; профиль строится заново для всех полос, а не только непринятых (161…357 мс против 127…259 мс после G3); цепочка итераторов вместо одного прохода в measureBandedPeriod.ts:825.

### Пороги охвата перекалиброваны на вырезке — решение по замеру диагноста

Замер на вырезке по контуру (то изображение, которое получает detectRuling): при нынешних порогах IMG_1805 теряется (охват 0,50, шаг 0 против 120,93), IMG_1804 висит на 0,75.
Причина не в кадре против вырезки, а в пороге контраста, стоящем вплотную к обрыву: у IMG_1805 крайние полосы дают 0,134 и 0,137 от медианы (у IMG_1800 те же полосы — 0,411 и 0,468) — у листа, снятого под углом, верх и низ почти без линий, и порог 0,15 режет ровно по ним. Вырезка убирает столбцы стола, медиана найденных полос растёт на 20 %, слабая полоса — на 6 %, и отношение переезжает 0,151 → 0,134, через порог.
Решение: MIN_BAND_CONTRAST_SHARE = 0,07 (середина плато вырезки 0,01…0,13: в 15 раз выше пустых полос ловушки 0,004 и вдвое ниже слабых полос худшего снимка 0,134), порог охвата 0,70 не меняется — по охвату окно одностороннее, любой порог из (0,60; 1,00] годится, ловушки держат ровно 0,60 при любом контрасте.
Отвергнуто: оставить 0,15 (теряем IMG_1805); мерить охват по кадру целиком (IMG_1805 проходит с запасом 0,001 от порога, то есть случайно, а цена — второй сдвинутый профиль по всему кадру на каждую полосу и стол в замере).
Находка на будущее: на реальном пути обе текстовые ловушки до полосовой ступени не доходят — контур режет их (480×400 → 480×11, 480×1200 → 480×605). Правило охвата проверяется юнит-уровнем на изображении без контура, и так и должно остаться: приёмка на вырезке проверяет другое.

### F2

Аудит F2: verdict ok, кругов 0.
Аудитор собрал свой стенд и воспроизвёл приёмку: на вырезке все десять снимков дают охват 1,00 и прежние шаги; при прежнем пороге 0,15 IMG_1805 отдаёт шаг 0. Рабочее окно контраста на вырезке — (0,004; 0,134): снизу его держит ловушка half-12 (при 0,004 проходит), сверху — слабейшие полосы IMG_1805 (0,134 и 0,137). Выбранные 0,07 — середина окна.
Ловушки на всём диапазоне 0,005…0,50 отказывают, охват у них не выше 0,60 — порог охвата остался 0,70 и не расшатан.
Дефект, признанный аудитором и принятый координатором к исполнению в G4: нижний край константы закрыт автотестом (ловушка half-12 краснеет при 0,004), верхний — нет, а он и есть причина правки: сдвиг до 0,14 молча теряет IMG_1805 при зелёном прогоне, живого теста на фотографиях быть не может (они в .gitignore). В G4 заводится синтетическая проверка потолка: лист, у которого в крайних полосах разбивки контраст линий сжат примерно до 0,13 от средних (сжатие — прямо в тесте, хелпер не правится, K4 цел), замер обязан отдать ненулевой шаг, а при 0,14+ тест краснеет.
Долг: в jsdoc «в пятнадцать раз ниже порога» при 0,07/0,004 даёт 17,5 — число округлено вниз.

### G4 · Полосовая ступень в `detectRuling`

Аудит G4: verdict ok, кругов 0. Главная проводка задачи закрыта.
Аудитор прогнал живые снимки сам: все десять отдают шаг (в опоре 0.00) — 60,13 · 136,11 · 129,64 · 134,04 · 121,68 · 120,97 · 86,23 · 87,35 · 90,88 · 87,67; перспектива измерена у девяти из десяти, у IMG_1705 нет. Восемь пресетов совпали с опорой до знака (6.1).
Сторож потолка порога из аудита F2 заведён и проверен мутацией: 0,07 → 0,14 роняет ровно его, соседние 16 тестов зелёные. Молчаливая потеря IMG_1805 теперь невозможна.
Эталоны «до бита» проверены подстановкой detectRuling с базы прогона — литералы сняты на базе, а не пересчитаны новым кодом. K6 держится (md5 profiles.json тот же), K2 соблюдён: полосы зовутся только при непойманном пороге, на пресетах время не выросло.
Правки вне первоначальных границ (разрешены координатором): отрицательный контроль в paper-measure-sheet-photo.test.ts переписан по существу — по кадру целиком вид разлиновки опознаётся неверно и левое поле уезжает больше чем на треть шага, то есть вырезка по-прежнему обоснована; в sheet-dialog.test.tsx снято одно устаревшее предусловие.
Отступления, принятые координатором: K2 расширен полем convergenceOrigin (без него K1 не выполняется); на полосовой ветке наружу уходит наклон, найденный полосами, — шаг, фаза и наклон идут одной тройкой.
Цена: пресеты 415—436 мс против опорных медиан 416—428 — в дизайн вписываемся. Десять снимков 1367—2339 мс против 610—1109 в опоре: раньше измерение бросало работу сразу после отказа по шагу, теперь проходит весь путь (односторонний мягкий критерий baseline.md).
Долг в G6: toFrameSource берёт фазу из полос, а наклон из проекции — сейчас расхождение 0,008…0,016°, но свип полос ограничен ±2°, а perspective.skewAngle у IMG_1808 равен −2,90°, и там фаза разошлась бы с углом на полшага; устаревший jsdoc confidence в paper.types.ts. Долг F1/F2: профиль охвата строится заново для всех полос — 161…357 мс против заложенных дизайном 0,2 с.

### Засев инертен — решение оставить

Замер исполнителя G6: подгонка перспективы сходится к той же гребёнке с засевом и без него — на всех десяти снимках (различия ≤1e-10) и на 20 синтетических листах дрейфа 25…100 %. Ломается она только от засева, завышенного в 35 раз.
Решение: засев остаётся в продакшене. Буква спеки выше простоты — design прямо предписывает «measureSheetPhoto передаёт засев в detectRulingPerspective», а задача 4.2 требует провести его через детектор; снятие полей convergenceSeed/convergenceOrigin вывело бы реализацию из-под спеки ради экономии двух чисел. Цена — два поля в DetectedRuling и один аргумент, расход нулевой.
Тест 4.2 написан на измеренное — «гребёнка с засевом и без засева одна и та же» плюс перенос чужого начала отсчёта, — а не на контроль из текста задачи («без засева тот же лист теряет перспективу»): такой контроль не воспроизводится ни на одном листе. Текст задачи 4.2 и соответствующее место design расходятся с замером — предложение правки артефактов уйдёт в итог прогона для /opsx:update.
IMG_1705 перспективы не получает, и засев тут ни при чём: невязка 13,86 px против лимита 12,03 (шаг/5) при доле узлов 0,943 и дрейфе 12,65 %. Шаг у снимка найден (60,13), в ручной ввод он не уходит — критерий issue #8 выполнен, лист описывается прямой гребёнкой.
Долги G4 закрыты этой же пачкой: фаза и наклон идут одной парой (второй проход отбрасывается, если его наклон не пара наклону выпрямления; порог выведен из спеки — сдвиг фазы не больше двадцатой шага при плече в полширины вырезки), jsdoc confidence переписан.

### G6 · Засев, диагностика, отчёт

Аудит G6: verdict ok, кругов 0.
Инертность засева перепроверена аудитором независимо — мутацией продакшена и перемером: наличие перспективы совпало у всех снимков, наибольшее расхождение чисел 2,3e-8; на синтетическом свипе дрейфа 0,25…1,00 результаты с засевом, без засева и с чужим началом отсчёта совпадают побитово, а засев, завышенный в 35 раз, перспективу теряет. Тест 4.2 пиннит измеренный факт, а не выдумку.
Числа после G4 не поехали: шаги десяти прежние, перспектива у девяти из десяти. Диагностика полос совпала до знака; отчёт печатает строку полос, у восьми пресетов — «полосы не понадобились», profiles.json бит-в-бит (K6).
Долги G4 закрыты по существу: наклон второго прохода спарен с фазой (гейт стоит только на втором проходе и имеет зубы), jsdoc confidence приведён в соответствие с кодом.
Долг: тест «чужое начало отсчёта засева не сбивает подгонку» зубов не имеет — выброс переноса начала оставляет тесты зелёными; это прямое следствие инертности, наблюдаемого способа проверить формулу k/(1+k·Δ) на этом уровне нет, и формулу нельзя считать проверенной. MAX_PHASE_SHIFT_SHARE зажат тестом широко (бракетинг 0,01° и 1° при допуске ≈0,2°). Список файлов G6 в plan.md стоит дописать: index.ts (реэкспорт типа для K3) и paper.types.ts (переданный долг G4).
Замечание по K5: перемер аудитора прошёл по всему каталогу, включая снимки сверх десяти; holdout.md не открывался, чисел сверх десяти ни в отчёт, ни в журнал не попало — удержанная выборка для G8 не засвечена.

### IMG_1705: разлиновка найдена, но мимо линий

Приёмка 7.1: девять снимков из десяти ложатся на разлиновку (медиана промаха 0,000…0,030 шага), IMG_1705 — нет. У него настоящий период линий уезжает 53 → 61 → 69 px по высоте блока (≈30 %), хранится один шаг 60,13, прямая гребёнка накапливает фазу и промахивает до полушага на 39 строках из 46. Перспектива у снимка отвергнута по невязке (13,86 против лимита 12,03) и сама недофитит: обещает дрейф 12,65 % против ≈30 % по снимку.
Итог: снимок перестал уходить в ручной ввод, но получил автоматическую разлиновку мимо линий — это хуже честного отказа. Полосы, к слову, видят у него дрейф 44,8 % против 4,3…19,5 % у остальных девяти, то есть противоречие «полосы против одной гребёнки» измеримо.
Решение координатора: сперва диагност выясняет, описывается ли IMG_1705 перспективой в принципе и почему подгонка недофитит. Если описывается — чиним подгонку (снимок остаётся в приёмке). Если нет — заводим отказ: на полосовой ветке дрейф полос выше гарантии при ненайденной перспективе означает, что одна гребёнка лист не описывает, и разлиновка не считается найденной (лист уходит в ручной ввод честно). Порогов под снимок не двигаем.

### IMG_1705: причина найдена, чиним геометрией

Диагност: подгонка не недофитит — ей скармливают не тот лист. Узлы берутся из полос по всей ширине вырезки, и крайняя полоса (x=2722) лежит на пружине блокнота: её «линии» — витки, 10 узлов с невязкой 21,6 px. Бюджет выбросов в 2 % — это один узел из 66, целую полосу он снять не может, отсюда отказные 13,86 px. Убрать эту полосу — невязка падает 23,3 → 3,5 px.
Модель лист описывает: общая подгонка по всем 46 строкам даёт максимум невязки 8,0 px при лимите 12,03, знаменатель в углах 0,87, а выпрямление найденной перспективой убирает дрейф полос 49 → 71 px до 58,3…60,3 px (1,5 %).
Решение: правим геометрию, а не пороги. (A) Полосы для перспективы берутся внутри области с линиями (столбцы margins.left…width−margins.right), а не по всей ширине вырезки — пружина, стол и переплёт в подгонку не попадают. (A2) Заданный угол на выпрямленной копии не переопределяется по вертикалям: на клетке вертикали сходятся, и копия получает угол 1,119° против 1,238° подгонки, что роняет пару фазы и наклона (сдвиг 3,14 px при лимите 3,01).
Отвергнуто: робастность по невязке («выбросить худшую полосу») — роняет три сторожа (прогиб с осью у правого края, сорванная трасса на половине кадра, дрейф 25 % с прогибом): по невязке «полоса вне бумаги» и «сорванная трасса» неотличимы, различать надо геометрией. Расширение окна строк на всю область письма — подгонка даёт честные 46,9 % дрейфа и упирается в MAX_STEP_DRIFT. Честный отказ (вариант B) остаётся запасным, если примирение тестов волны окажется дороже группы; «как есть» (C) отвергнут — медиана промаха 0,52 шага, максимум 1,68.
Ожидаемый итог A+A2: IMG_1705 ложится на все 46 строк с медианой промаха 0,027 шага и максимумом 0,35.

### F3

Аудит F3: verdict ok, кругов 0. Главная находка приёмки закрыта.
IMG_1705 ложится на линии: шаг 59,35, перспектива и изгиб найдены, промах базовых линий медиана 0,001—0,002 и максимум 0,006—0,008 (до правок 1,41 и 1,96 — текст шёл мимо линий). Аудитор мерил сам, по минимумам профиля пикселей, а не по модели.
Девять остальных не съехали, поля у двух снимков сдвинулись на шаг — проверено строго и оказалось исправлением, а не дефектом: у IMG_1809 прежнее верхнее поле указывало на линию, которой на бумаге нет (профиль там монотонно светлеет), новое садится на первую настоящую линию; у IMG_1801 добавленная строка стоит на настоящей линии.
Зубы всех трёх правок проверены мутациями; тексты фикстур «волна у верха листа» не правились, файл в диффе отсутствует — тесты зелёные по существу. K6 держится.
Граница приёмки по живым снимкам, заданная координатором (вопрос аудита): медиана промаха не выше 1/20 шага и максимум не выше 1/4 шага. IMG_1809 с максимумом 0,154…0,200 проходит; гарантия спеки «двадцатая шага в любой точке» описывает восстановление линий моделью перспективы (на синтетике 0,02…0,04 px), а не всю цепочку на фотографии, где изгиб отвергнут. Тем же критерием меряется удержанная выборка в G8.
Долг: после A2 гард isSkewPaired в продакшене недостижим — обе ветки detectRuling отдают заданный угол, зубы остались только у мока; сторож оставлен по решению координатора, он дёшев и стережёт тот же инвариант для будущих путей. Ещё: `??` без комментария в detectRuling.ts:1583 (code-style #6).

### G7 · Пресет-пак, приёмка по фото, гейт

Аудит G7: verdict ok, кругов 0. Правок в репозитории группа не делала — её продукт в /tmp/perspective-ruling-detection/acceptance-7.4.md и raw-7.4/.
Аудитор перемерил сам: шаги десяти снимков совпали до сотой, перспектива у 10/10, попадание базовых линий по минимумам профиля — медиана 0,001…0,048 шага. Зубы проверки: контраст на предсказанной линии 0,05…0,31 против −0,013…+0,002 в середине между линиями, растяжка шага на 3 % поднимает промах до 0,21…0,33 — «прилипания» нет. Пресет-пак бит-в-бит, время пресетов по его замеру даже ниже опоры (−1,7…−0,2 %), у десяти отказных +105 % (они теперь проходят весь путь). Детерминизм — 48 пар повторов и независимый четвёртый прогон, ноль расхождений.
Решения координатора по вопросам аудита:
1) IMG_1809 — промах не шум, а систематическая дуга (0,10 вверху, 0,02—0,03 в середине, 0,17 внизу, отдельные пробы у левого края до 0,35): границу из F3 проходит (медиана 0,048, построчный максимум 0,17), запас по максимуму меньше трети — число заносится в журнал и в тело PR, а не замалчивается.
2) IMG_1705 — последние три базовые линии из 48 ложатся за концом области с линиями (глубина профиля 0,00 против 0,15 на строках 0—44, нижнее поле на ~3 шага ниже последней настоящей линии). Спеку это не нарушает: за поля текст не заходит. Принято как известное ограничение и вынесено в тело PR; чинить границу области по трассе линий в этом change не буду — это отдельная задача, и правка задела бы поля у всех листов, включая пресеты.
3) Время импорта 1,8—2,3 с на кадр 3024×4032 при синхронном вызове из хука: буква 7.4 «время у снимков, которые измерялись и раньше, не выросло» выполнена (пресеты не выросли), рост только у тех, что раньше отказывали сразу, и он пред-разрешён правилом сверки в baseline.md. Верхней границы не задано нигде — выношу вопросом в тело PR.
4) Сценарий «Повторный импорт даёт те же числа» — новый SHALL без наблюдающего теста в репозитории, то есть пустая клетка для итогового аудита. Заводится задачей в G8: дважды позвать measureSheetPhoto на синтетическом листе с дрейфом и сверить результат через toStrictEqual.
Долг: в acceptance-7.4.md по 7.1 приведена только медиана промаха, максимума нет, хотя граница приёмки задана парой.

### Удержанная выборка вскрыта: одна находка

G8 открыла holdout.md (контракт K5 держался всеми группами до этого момента). Три снимка из четырёх чисты: шаг не потерян, наибольшее изменение 0,019 % при допуске 2 %, попадание базовых линий 0,000…0,005 медиана и 0,005…0,015 максимум при границе 0,05/0,25 — ровно как до правок. Нижние поля у IMG_1806 и IMG_1813 опустились на шаг, добавленные строки лежат на настоящих линиях. Подгонки под десятку приёмки удержанные не показали.
Находка: IMG_1596 получил линию поля, которой на снимке нет («линии поля нет» до правок → «1066,9 px, left» после). В этом месте вертикали нет — контраст к фону −0,0003 при настоящих вертикалях клетки 0,019…0,043 и пороге 0,03; лист школьный, цветного поля на нём нет вовсе. Цена для пользователя тяжёлая: блок сужается с 2277 до 1532 px, текст начинается на трети ширины листа и теряет треть строки.
Решение координатора: это регресс, внесённый прогоном, а не известное ограничение снимка, поэтому чиним, а не выносим в PR. Сначала диагност ищет причину — почему линия поля принимается там, где контраст на два порядка ниже порога; правка идёт отдельной группой F4 с обязательной проверкой K6 и перемером всех четырнадцати живых снимков (десять приёмки плюс четыре удержанных).

### F4

Аудит F4: verdict ok, кругов 2. Группа закрывает регресс, найденный удержанной выборкой.
IMG_1596 получал линию поля, которой на листе нет: второй проход искал её при наклоне подгонки 0,408°, и вертикаль клетки перешагнула барьер с запасом 1,6 % (на 0,40° и 0,42° отказывает). Блок сужался с 2277 до 1532 px — треть строки.
Правка по кругам: первый ставил снятие фантома после прохода, и аудит нашёл critical — линия успевала обрезать сетку изгиба, блок расширялся, сетка нет, промах базовых линий на освобождённой трети 0,130/0,245. Второй круг перенёс решение в измерение. Третий убрал последнюю правку результата: сторона ровного прохода уходит в детектор параметром marginLineSide, findMarginLine выбирает кандидата стороной, а не сравнением глубин, inheritMarginLine удалён целиком (rg по src и tests — ноль совпадений). Ветка «чужая сторона» теперь недостижима устройством, а не правилом: линия, обрезавшая сетку, и линия в разлиновке — всегда одна.
Путь по умолчанию не тронут доказуемо: при незаданной стороне выражение тождественно прежнему Math.max, барьер и его числа те же.
Итог по 26 живым снимкам: изменились ровно два — IMG_1596 и IMG_1812 потеряли линии поля. Обе фантомные: аудитор проверил зумом, что 1653,9 у IMG_1812 — ровно начало правой трети окна поиска, а настоящая красная линия того листа стоит слева и не находится ни до, ни после. Снято два фантома, потеряно ноль настоящих линий. Промах базовых линий у обоих 0,000/0,005 при границе 0,05/0,25. Десятка приёмки цела, три остальных удержанных снимка не изменились, profiles.json бит-в-бит (K6).
Долг: в тесте сетки изгиба при переводе в it.each проверка ослаблена — вместо «линии поля нет» стоит «сторона другая»; строгая форма бесплатна. Ещё: IMG_1813 держит фантомную линию 1640,7 right, пришедшую от ровного прохода ещё на базе прогона — вне этого change, но пустая клетка того же класса.
