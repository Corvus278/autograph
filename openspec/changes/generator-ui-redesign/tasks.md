## 1. Модель: рецепт до картинки и перестройка стора

- [x] 1.1 Разделить стор на документ, просмотр и ресурсы (D3). Заменить `inkColor` + `isInkColorAuto` на `ink`, flags и частоты — на `realism`. Удалить `seed`, `fontSize`, `blockWidth`, `lineSpacing`, `topOffset`, `leftPadding`, `blockRotate`, `backgroundId`, `customBackgroundSrc`, `selectBackground`, `setCustomBackground`. Проверка: `npm run typecheck`, `tests/generator-store.test.ts` обновлён и зелёный.
- [x] 1.2 Перевести сборку параметров страницы (`buildPageRenderParams`, `usePageRender`, `useRunRender`) на `recipe.inkColor` и `recipe.handwriting` (D4). Проверка: юнит-тест — при `ink.kind = 'auto'` цвет на странице равен `recipe.inkColor` и меняется с `runSeed`; при `tone`/`custom` не меняется. Второй тест — правка текста не меняет seed почерка в параметрах страницы.
- [x] 1.3 Сделать `regenerate` новым прогоном (`startNewRun`): правка текста и реализма `runSeed` не трогает. Проверка: юнит-тесты стора и существующие тесты `run-recipe` («Повторяемость прогона», «Явная перегенерация») зелёные.
- [x] 1.4 Завести `config/realismLevels.ts` (D5): ступени «Ровно», «Аккуратно», «Обычно», «Небрежно», дефолт «Обычно». Экшены `selectRealismLevel` и правок отдельных полей ставят `level: 'custom'`. Проверка: юнит-тест — ступень пишет свои значения, «Ровно» выключает все искажения, правка одного флага даёт `custom` и не трогает остальные.
- [ ] 1.5 Форматтеры единиц в `lib/format/`: доли шага со знаком, градусы, px, проценты, словесная шкала частот 1–5. Проверка: юнит-тесты на каждый форматтер, включая ноль и отрицательные.

## 2. Сессия и история правок

- [ ] 2.1 `model/sessionSchema.ts`: по-полевая проверка сохранённой сессии с дефолтом для негодного поля (D7). Проверка: юнит-тест на каждое поле — неверный тип, выход из диапазона, неизвестный `toneId` и `level`, не-JSON, чужая версия.
- [ ] 2.2 Подключить `persist` с ключом `autograph.session`, `partialize` документа и `isSpread`, дросселированной записью и хранилищем, глотающим ошибку записи. Проверка: компонентный тест — сессия восстанавливается после пересоздания стора; при `setItem`, бросающем `QuotaExceededError`, генератор работает.
- [ ] 2.3 Проверять ссылки на листы после восстановления своих листов и профилей: пропавший закреплённый лист снимает закрепление, неизвестная семья заменяется первой. Проверка: юнит-тест на оба случая, остальные поля сохраняются.
- [ ] 2.4 История правок (D8): `past`/`future`, глубина 100, `commit` со склейкой по `coalesceKey`. Текст склеивается в окне ~800 мс, слайдеры коммитят шаг на `onValueCommit`. Просмотр и ресурсы в историю не попадают. Проверка: юнит-тесты — склейка набора, разрыв после паузы, отмена перегенерации возвращает `runSeed`, новая правка очищает `future`, переполнение теряет старейший шаг, `pageIndex` не в истории.

## 3. Токены и примитивы

- [ ] 3.1 Токены в `@theme` по таблице D2 (тёмная тема, `oklch`, область просмотра на ступень светлее панелей), `min-width` через `--spacing-desktop`, `--color-neutral-850` заменить семантическими токенами. Проверка: `npm run lint` (stylelint + prettier) зелёный; край светлого листа читается на фоне области просмотра в story; `rg "violet-|zinc-|neutral-850" src` пусто после группы 5.
- [ ] 3.2 Перевести существующие примитивы `shared/ui` на токены, `Slider` → `ValueSlider` с подписью значения и единицей. Проверка: stories примитивов проходят `a11y` в `npm run test:stories`.
- [ ] 3.3 Новые примитивы `SegmentedControl`, `TileRadio`, `Swatch`/`SwatchGroup`, `Dialog`, `Disclosure`, `IconButton`, `Toolbar` на Radix; добавить `@radix-ui/react-dialog`, `@radix-ui/react-toggle-group`, `@radix-ui/react-collapsible`. Проверка: у каждого story и тест клавиатуры в `tests/ui-primitives.test.tsx` (стрелки в группах, Esc закрывает диалог, фокус возвращается на кнопку-триггер).
- [ ] 3.4 `widgets/AppHeader` со слотом действий страницы; правило для `widgets` в `steiger.config.ts`, если нужно. Проверка: `npm run lint:fsd` зелёный, story шапки.

## 4. Экран генератора

- [ ] 4.1 Каркас экрана (D1): шапка, три колонки на высоту окна, полоса действий, независимая прокрутка колонок. Проверка: story в Chromium 1440×900 — полоса действий в окне при раскрытом экспертном режиме, прокрутка настроек не двигает лист.
- [ ] 4.2 `TextPane`: поле на всю высоту, счётчик знаков и число страниц. Проверка: компонентный тест — ввод обновляет число страниц.
- [ ] 4.3 `ActionBar`: «Перегенерировать», «Сохранить страницу», «Скачать все» с прогрессом «Готово N из M» и отменой, недоступность без текста с видимой причиной. Перенести проверки из `tests/save-bar.test.tsx` и `tests/batch-export.test.tsx`: имя файла, ошибка в `role=alert`, ошибка отдельной страницы, отмена. Удалить старые файлы в этой же задаче. Проверка: новый `tests/action-bar.test.tsx` зелёный.
- [ ] 4.4 `SheetViewport`: основной растр в масштабе `min(zoom, fitZoom) × DPR`, вписывание через `ResizeObserver`, лестница масштаба 10–400 %, Ctrl/⌘+колесо, прокрутка и панорама пробелом, подпись масштаба в процентах кадра. Проверка: юнит-тест `fitZoom` и лестницы; компонентный тест — «Вписать» возвращает `'fit'`, подпись процентов.
- [ ] 4.5 Детальный растр через воркер при `zoom × DPR` выше основного: debounce, отмена по `AbortSignal` на новую правку (D6). Проверка: компонентный тест с поддельным клиентом воркера — заказ после остановки зума в нужном масштабе, отмена при правке, без заказа при «Вписать».
- [ ] 4.6 Разворот: пара `2k−1`/`2k`, непарная последняя слева, навигация шагом 2, текущая страница подсвечена, клик выбирает её для сохранения. Проверка: компонентный тест на четыре и три страницы; story разворота — чётная страница зеркальна.
- [ ] 4.7 Навигация: «‹ N / M ›», переход по номеру, стрелки вне полей ввода, сдвиг на последнюю при сокращении текста. Перенести проверки из `tests/page-preview.test.tsx` и заменить в нём проверку ширины `PAGE_WIDTH`. Проверка: компонентные тесты стрелок в поле текста и вне его.
- [ ] 4.8 Кнопки undo/redo в шапке и клавиши Ctrl/⌘+Z, Ctrl/⌘+Shift+Z, Ctrl+Y, в том числе в поле текста с `preventDefault`. Проверка: компонентный тест — набор и отмена одним шагом, отмена перегенерации, кнопка отмены недоступна после загрузки.

## 5. Настройки оформления

- [ ] 5.1 `PaperPicker`: плитки семей с изображением первого экземпляра и подписью, плитка «+ Своё фото», свои листы плитками с кнопкой настройки. Выбор семьи снимает закрепление. Проверка: компонентный тест выбора и снятия закрепления.
- [ ] 5.2 `HandwritingPicker`: список с названиями своим начертанием, подключение своего шрифта, сообщение о битом файле без смены почерка, подсказка со ссылкой на «Как создать свой шрифт». Проверка: перенесённый тест битого шрифта и тест, что у пунктов стоит `font-family` шрифта.
- [ ] 5.3 `InkPicker`: «Авто» первым, свотчи `INK_PALETTE` с названиями в подсказке и `aria-label`. Проверка: компонентный тест — выбор тона пишет `ink.kind = 'tone'`, «Авто» — `auto`, после перегенерации ручной цвет на странице не меняется.
- [ ] 5.4 `RealismPicker`: сегментированный переключатель ступеней с состоянием «Свой». Проверка: компонентный тест — дефолт «Обычно», правка флага в экспертном режиме показывает «Свой»; подобрать числа ступеней на глаз по story и записать итог в таблицу D5.
- [ ] 5.5 `ExpertSettings` в `Disclosure`, свёрнут по умолчанию: поправка геометрии и запас снизу, флаги и частоты искажений словами шкалы, вариативность контуров, закрепление экземпляра, произвольный цвет, сцена (параметры скрыты при выключенной). Выяснить единицы параметров сцены в `lib/export` и закрыть Open Question в `design.md`. Проверка: компонентный тест — все перечисленные в спеке контролы доступны, у каждого числа есть единица.
- [ ] 5.6 `SheetDialog`: фото листа, поля границ и разлиновки с черновиком, «Перемерить», «Удалить» с подтверждением, применение только по «Сохранить», автооткрытие при неудаче автоопределения, индикатор разбора. Перенести проверки из `tests/paper-group-import.test.tsx`, `tests/paper-group-remeasure.test.tsx` и `tests/paper-group-ruling-edit.test.tsx`, включая сохранение изгиба и перспективы при совпадении шага и фазы, клемп полей перемером и «Лист без разлиновки». Удалить старые файлы в этой же задаче. Проверка: новый `tests/sheet-dialog.test.tsx` зелёный.
- [ ] 5.7 Удалить `SettingsPanel`, `PagePreview`, `PageNav`, `SaveBar` и их stories, `tests/settings-panel.test.tsx`. Проверка: `rg "SettingsPanel|SaveBar|PageNav" src tests` пусто, `npm test` зелёный.

## 6. Другие экраны

- [ ] 6.1 CreateFont и NotFound на `AppHeader` и токенах, без изменения контента. Проверка: `tests/routes.test.tsx` обновлён (шапка на всех трёх экранах, переходы) и зелёный.

## 7. Проверка целиком и документация

- [ ] 7.1 Перевести stories `BaselineFit`, `RealLayout`, `RasterRuling` на `SheetViewport`; добавить stories экрана целиком, разворота, диалога листа, экспертного режима. Проверка: `npm run test:stories` зелёный, включая `a11y`.
- [ ] 7.2 Переснять скриншотные эталоны в docker, удалить эталоны удалённых stories. Проверка: `npm run test:visual:update`, затем `npm run test:visual` зелёный; просмотреть новые эталоны глазами.
- [ ] 7.3 Ручная проверка в браузере по сценариям спек: перезагрузка сохраняет сессию, зум 200 % резкий, разворот, пачка с отменой, undo перегенерации. Проверка: пройти в `npm run dev`, результат записать в отчёт по задаче.
- [ ] 7.4 Обновить `CLAUDE.md`: «Структура» (ui, `widgets`, `lib/format`), стор из трёх частей, сессия и история, «Тесты». Проверка: `npm run lint` и `npm test` зелёные, 0 warnings в новом коде.
