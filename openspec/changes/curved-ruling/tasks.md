## 1. Модель изгиба

- [x] 1.1 Добавить в `paper.types.ts`:
  - тип `RulingBend` (`columnOrigin`, `columnSpacing`, `columnCount`, `rowOrigin`, `rowSpacing`, `rowCount`, `offsets`);
  - обязательное `bend: RulingBend | null` в `SheetRuling` и в `RulingDetection`;
  - необязательное `bend?: RulingBend | null` в `SheetRulingSource`.

  `buildSheetRuling` переносит изгиб и при `step ≤ 0` отдаёт `null`. `mirrorSheetRuling` пока переносит `bend` как есть: до 1.3 он всегда `null`. Проставить `bend: null` во всех литералах разлиновки и детекции:
  - `lib/paper/detectRuling.ts` (`toMissingDetection` и результат детекции), `lib/paper/mirrorSheetRuling.ts`, `model/paperSheetJson.ts`, `ui/Generator/SettingsPanel/PaperGroup/useSheetImport/useSheetImport.ts`, `ui/Generator/PagePreview/BaselineFit.stories.tsx`;
  - `tests/helpers/paper-family.ts`;
  - тесты `calibrate-geometry`, `page-geometry`, `page-render-params`, `paginate`, `paper-profiles`, `paper-sheet-ruling`, `paper-sheet-ruling-mirror`, `store-geometry-correction`, `store-user-sheets`, `stored-user-sheets`;
  - прочее — по выводу `npm run typecheck`.

  Проверка — `npm run typecheck` и `npm test` проходят; юнит-тест `buildSheetRuling` сохраняет переданный изгиб, даёт `null` без него и `null` при нулевом шаге.
- [x] 1.2 Написать `sampleRulingBend(bend, skewAngle, x, y)` и производную `∂d/∂x` вдоль линии (при постоянной `u`) в `lib/paper/`: равномерный Катмулл — Ром по узлам строки с фантомными узлами `P₋₁ = P₁`, `P_N = P_{N−2}`, линейно между строками по координате вдоль линий, константа за крайними узлами. Проверка — юнит-тесты:
  - значение в узле равно узлу, между узлами строки кривая проходит через узлы;
  - за крайними узлами — значение ближайшего узла, производная непрерывна при переходе через крайний узел (конечные разности слева и справа сходятся к нулю);
  - на наклонном листе точка на линии гребёнки получает смещение своей строки узлов;
  - производная совпадает с конечной разностью вдоль линии.
- [x] 1.3 Научить `mirrorSheetRuling` отражать изгиб (`rowOrigin + tgθ·width`, `columnOrigin′ = width − последний узел`, узлы строки в обратном порядке). Проверка — юнит-тест на наклонном изогнутом листе с несимметричной сеткой (область с линиями смещена к одному краю): положение линии, восстановленное по отражённой разлиновке в нескольких `x`, совпадает с положением той же линии, отражённой вручную, с точностью до сотой шага. Двойным отражением не проверять.

## 2. Измерение границ и изгиба

- [x] 2.1 Снять базовый замер времени `detectRuling` на синтетике 3000×4000 до первой правки детектора. Проверка — время записано в журнал прогона вместе с коммитом, на котором снято.
- [x] 2.2 Расширить `tests/helpers/synthetic-sheet.ts`:
  - функция смещения горизонтальных линий от точки листа;
  - изогнутая линия поля, изогнутые крайние вертикали клетки и изогнутые концы горизонтальных линий;
  - периодические тёмные пятна спирали с шагом, отличным от шага линий, ближе шага от концов линий, и линейка другого шага за линией поля;
  - пятно пониженного контраста линий на части ширины.

  Экспортировать эталонный изгиб для сверки. Проверка — юнит-тест хелпера: центр нарисованной линии в нескольких столбцах лежит на заданном смещении, конец линии — на заданной границе.
- [x] 2.3 Прослеживать найденную линию поля по горизонтальным полосам высотой около полутора шагов (не меньше 8):
  - старт в полосе с самым глубоким провалом в окне ±⅓ шага вокруг среднего;
  - дальше окно ±⅙ шага от соседней полосы;
  - узел принимается при глубине не меньше 0,8 стартовой;
  - медиана по трём соседним полосам, итог — самое внутреннее положение, не наружу от среднего.

  Проверка — юнит-тесты на синтетике:
  - линия поля слева и справа с параболическим изгибом 0,3 шага в двух вариантах — самая внутренняя точка у верха и низа области и самая внутренняя точка в середине высоты: `marginLineX` не дальше от области письма, чем самая внутренняя точка линии, с допуском в пиксель;
  - прямая линия поля даёт прежний результат;
  - линия, отсутствующая в части полос, уточняется по остальным;
  - на клетчатом листе трасса не уходит на обычную вертикаль.
- [x] 2.4 Уточнять крайние вертикали клетки в `findGridColumns` той же трассировкой по полосам высотой около полутора шагов вместо окна вокруг прямой гребёнки. Проверка — юнит-тест сценария «Изогнутая граница сетки»: на синтетике без линии поля с изогнутой крайней вертикалью левое и правое поле не ближе к краю кадра, чем самая внутренняя точка вертикали плюс пятая часть шага; прежние тесты границы сетки проходят.
- [x] 2.5 Добавить в `lib/paper/sheetProfile.ts` полосный отклик гребёнки:
  - полосы по координате вдоль линий `u = y − x·tgθ`, границы посередине между линиями, высота — целое число шагов, не меньше двух;
  - из яркости вычитается среднее бина по полосе;
  - защитная полоса наклона — по высоте полосы, а не кадра;
  - отклик — модуль квадратур `√(C² + S²)`.

  Считать по нему границу концов горизонтальных линий в каждой полосе внутри найденной по высоте области с линиями:
  - обрыв в полосе принимается, только если отклик за ним ниже половины общей для всех полос девятой децили, иначе полоса не голосует;
  - медиана по трём соседним полосам;
  - итог — самое внутреннее положение, не наружу от нынешнего измерения.

  Проверка — юнит-тесты на синтетике линейки без линии поля:
  - сценарий «Изогнутые концы линий»: концы линий на разном отступе по высоте — боковое поле не ближе к краю кадра, чем самый внутренний конец плюс пятая часть шага;
  - горизонтали изогнуты на 0,3 шага в углах при ровных концах — поле прежнее ±1 px;
  - наклон ±1,5° при ровных концах — поле прежнее ±1 px;
  - периодические пятна спирали из 2.2 и яркостная ступень за концами линий — поле прежнее ±1 px;
  - пятно пониженного контраста линий на 3–4 соседние полосы у одного края — поле прежнее ±1 px;
  - концы линий в двух шагах от края кадра высотой 4000 px при наклоне 1,5° — конец найден с точностью до пикселя;
  - ровный лист с ровными концами — поле прежнее ±1 px.
- [x] 2.6 Сверить поля пресетов после 2.3–2.5. Проверка:
  - до правок 2.3–2.5 поля и линия поля каждого листа сняты эталоном (`jq` по `public/paper/profiles.json`) в журнал прогона;
  - `npm run build:paper` прогнан, поля и линия поля каждого листа по печати скрипта или `jq` отличаются от эталона не больше чем на пиксель, расхождения разобраны и записаны в журнал;
  - `public/paper` (артефакт и карты текстуры) возвращён `git checkout`, `git status` по нему чистый; артефакт окончательно пересобирается в 3.4.
- [x] 2.7 Написать `lib/paper/detectRulingBend.ts`. Вход — изображение, прямая разлиновка и горизонтальная область с линиями `left…right`.
  - Полосы: `N = max(8, round((right − left) / (2·step)))` равных полос шириной около двух шагов, без верхнего предела; узлы в центрах полос, за областью узлов нет.
  - Старт с ближайшей к центру полосы, где линия нашлась в окне ±⅓ шага.
  - Трассировка к краям области в окне ±⅙ шага вокруг предсказания.
  - Фильтр выбросов: узел заменяется медианой соседних линий в столбце, только если отличается от неё больше чем на ⅒ шага; у первой и последней строки узлов — медианой трёх ближайших строк внутрь области.
  - Заполнение пропусков, округление смещений до сотых пикселя.

  Проверка — юнит-тесты на синтетике:
  - перспектива 0,3 шага в углах, прогиб по ширине, местная волна амплитудой 0,1 шага и длиной 8–10 шагов, ровный и наклонный ±1,5° лист, шум, пятно на линиях центральной полосы;
  - линии кончаются внутри кадра;
  - восстановленное положение каждой линии внутри области отличается от эталона не более чем на двадцатую шага, все смещения кратны 0,01.
- [x] 2.8 Добавить проверку надёжности. Изгиб — `null`:
  - если найдено меньше 60 % узлов области;
  - если фильтр заменил больше 20 % найденных узлов;
  - при смещении от половины шага;
  - при скачке больше ¼ шага между соседними строками узлов;
  - при изгибе меньше двадцатой шага.

  Проверка — юнит-тесты:
  - `null` дают ровный лист, лист с изгибом ниже порога, лист с линиями, видимыми на трети площади, и лист с тёмными посторонними полосами поперёк линий;
  - лист из 2.7 изгиб сохраняет.
- [x] 2.9 Вызвать измерение изгиба из `detectRuling` после боковых границ: область — между концами линий, со стороны линии поля — до неё. Отдавать изгиб в `RulingDetection.bend`. Проверка:
  - юнит-тест: разлиновка, собранная `buildSheetRuling({ ...detection })` на изогнутом синтетическом листе, несёт изгиб;
  - юнит-тест: изогнутый лист с концами линий внутри кадра, пятнами спирали у края и линейкой другого шага за линией поля — изгиб сохраняется, точность внутри области не хуже двадцатой шага;
  - прежние тесты `paper-detect-ruling.test.ts` проходят без правки ожиданий.
- [ ] 2.10 Повторить замер времени `detectRuling` из 2.1 после 2.3–2.9. Проверка — рост времени записан в журнал прогона; если он больше 50 %, в этой же задаче делается общий проход по полосам для изгиба, `findRuledSpan` и границ.

## 3. Импорт, правка, хранение, профили

- [x] 3.1 Научить `model/paperSheetJson.ts` разбирать `bend`: размеры, не согласованные с длиной смещений, неположительные интервалы, нечисловые значения и отсутствие поля дают `null`. Проверка:
  - юнит-тесты: запись с изгибом, запись без изгиба и запись с испорченным изгибом — лист остаётся в списке, в последних двух случаях ровным;
  - тест `store-user-sheets`: лист с изгибом после записи в хранилище и чтения обратно несёт изгиб, строго равный исходному.
- [x] 3.2 Проверить, что `useSheetImport` сохраняет изгиб из детекции в своём листе. Проверка — компонентный тест импорта изогнутой синтетической фотографии: у добавленного листа `ruling.bend` не `null`, после перечитывания стора из хранилища изгиб тот же.
- [x] 3.3 Передавать `bend` в `PaperGroup.handleRulingApply` явно. Изгиб остаётся, если шаг и фаза из формы равны исходным, округлённым до сотых так же, как их показала форма, и сбрасывается в `null` иначе. Проверка — компонентный тест `paper-group-ruling-edit` на листе с шагом `47,9312` и фазой `19,2587`:
  - правка одного поля оставляет прежний изгиб;
  - правка шага даёт `bend: null`;
  - правка фазы даёт `bend: null`.
- [x] 3.4 Писать изгиб в `scripts/build-paper-profiles.ts` и печатать по каждому листу наибольший отход в долях шага и долю найденных узлов. Пересобрать `public/paper/profiles.json` без подъёма версии. Проверка:
  - `npm run build:paper` проходит, напечатанные величины записаны в журнал прогона;
  - поля и линия поля совпадают со сверенными в 2.6;
  - `tests/paper-profiles.test.ts` проходит, артефакт читается приложением.

## 4. Отрисовка

- [x] 4.1 Добавить `bend` в `RenderGeometry` и передавать изгиб разлиновки страницы из `buildPageRenderParams`, на чётной странице — отражённый. Проставить `bend: null` в литералах геометрии отрисовки:
  - `ui/Generator/PagePreview/BaselineFit.stories.tsx`;
  - тесты `batch-export`, `ink-layer-render`, `page-render-params`, `page-render-worker`, `page-render`, `render-page-canvas`, `render-page-glyphs`, `save-bar`;
  - прочее — по выводу `npm run typecheck`.

  Проверка:
  - юнит-тест `page-render-params`: на нечётной странице в параметрах изгиб листа, на чётной — результат `mirrorSheetRuling`;
  - тест `page-render-worker`: записыватель порта кладёт `structuredClone(request)`, и изгиб в клоне равен исходному.
- [x] 4.2 Вести в `renderPageToCanvas` параллельную аффинную матрицу стека трансформаций (без `scale`) и рисовать точки контуров как `p + L⁻¹·(0, d(M·p))`, только при `bend !== null`. Наклон для выборки — `blockRotate`. Проверка:
  - тест по `tests/helpers/path-recorder.ts` на изогнутом листе: низ тела букв в каждом столбце строки лежит на линии разлиновки с изгибом с отклонением не больше десятой шага, включая длинное слово через подъём линии и строку с поворотом неровности почерка;
  - прежние тесты ленты вызовов (`render-page-canvas`, `render-page-glyphs`, `page-render-params`) проходят без правки ожиданий;
  - шпион на `sampleRulingBend` через `vi.mock` с `importOriginal` (образец — `tests/store-run-recipe.test.ts`) при `bend: null` не вызывается.
- [ ] 4.3 В режиме изгиба рисовать слово без контуров (`glyphs: null`) и букву без контура по одной: ширины — из `measureText`, сдвиг `L⁻¹·(0, d)` и поворот на `atan(∂d/∂x)` вдоль линии вокруг середины буквы на базовой линии. Проверка — тест по `path-recorder`/`canvas-recorder` с `glyphs: null`:
  - на изогнутом листе каждая буква нарисована отдельным `fillText`, её точка на базовой линии в пикселях страницы отстоит от линии разлиновки не больше десятой шага, поворот равен касательной;
  - на ровном листе слово рисуется целиком одним `fillText`, как сейчас.
- [ ] 4.4 Проверить связность почерка на изогнутом листе. Проверка — тест числа связных областей чернил (как в `render-page-glyphs.test.ts`): на изогнутом листе с включённой вариативностью оно совпадает с тем же текстом на ровном листе.
- [ ] 4.5 Проверить детерминизм и независимость раскладки от изгиба. Проверка:
  - тест по образцу `page-render-params.test.ts` (два записывателя вызовов, одни параметры): два прогона на изогнутом листе дают одинаковую ленту;
  - модуль выборки изгиба добавлен в `RENDER_PATHS` в `tests/render-determinism.test.ts`, скан проходит;
  - юнит-тест раскладки: один и тот же текст на листе с изгибом и без него даёт одинаковые страницы, а отпечаток кэша `measureLayout` не меняется.
- [ ] 4.6 Проверить, что поправка геометрии сдвигает изогнутые строки. Проверка — тест по `path-recorder`: с поправкой `topOffset` в долю шага низ тела букв сдвинут на эту долю, а изгиб строки по столбцам сохраняется.

## 5. Stories, документация, приёмка

- [ ] 5.1 Перевести арифметику `BaselineFit.stories.tsx` на положение линии с изгибом: попадание проверяется в нескольких точках строки от левого до правого края блока, на нечётной и зеркальной странице. Отрицательный контроль: на пресете с наибольшим отходом больше десятой шага тот же расчёт без изгиба даёт дрейф у края больше допуска. Проверка — `npm run test:stories` проходит; если ни у одного пресета после 3.4 отход не больше десятой шага, отрицательный контроль пропускается с записью в журнал прогона, а сквозное попадание держит 5.2.
- [ ] 5.2 Добавить в `RasterRuling.stories.tsx` генератор изогнутого листа прямо в файле story (хелперы `tests/` из `src` недоступны):
  - изгиб зависит только от `x`;
  - изгиб листа находит `detectRuling` на его растре, и story числом проверяет, что найденный изгиб в центрах крайних полос не меньше 0,25 шага (форма изгиба подбирается под это условие);
  - лист подкладывается в семью стора, фото — `loadRenderImage` из data URL.

  Замер идёт по узким полосам по `x`, не шире трёх шагов: левая, средняя, правая. В каждой полосе два замера:
  - `measureDrift`: базовая линия геометрии (`buildBaselines`) плюс `sampleRulingBend` найденного изгиба в центре полосы против линии растра;
  - низ чернил строки против линии растра.

  Проверки пресетов (`expectRasterOnRuling`, `checkFamily`) переходят на те же полосы. Проверка — `npm run test:stories`:
  - на изогнутом листе на нечётной и на зеркальной странице дрейф базовых линий в каждой полосе не больше десятой шага (`DRIFT_TOLERANCE`), а отклонение низа чернил — в пределах допуска чернил story;
  - в каждой полосе есть чернила каждой проверяемой строки;
  - отрицательный контроль в той же story: тот же лист, отрисованный с `bend: null`, даёт в крайних полосах отклонение низа чернил больше допуска;
  - замер низа чернил проходит и для варианта без контуров (шрифт из 5.3), сценарий «Свой шрифт повторяет изгиб»;
  - пресеты проходят на новом замере.
- [ ] 5.3 Расширить `TextStaysOnPaper`. Проверка — stories проходят:
  - проверяются оба края: нет чернил на верхней и на нижней кромке кадра, низ строчного бокса последней строки в кадре;
  - добавлен вариант без контуров: шрифт пака регистрируется через `FontFace` под `CUSTOM_FONT_FAMILY` (`findFontUrl` для него отдаёт `null`), `params.glyphs` равен `null`, свес буквы берётся из `measureText` (`actualBoundingBoxLeft`/`Right`); правятся места, завязанные на контуры: ожидание `glyphs`, сверка шрифта с `DEFAULT_FONT` в `waitForProbe`, `buildOverhangBoxes`;
  - оба варианта гоняются на пресетах и на изогнутом листе из 5.2 с проверкой `params.geometry.bend !== null`.

  Если бокс выходит за кадр или буквы выступают за край блока больше свеса, это конфликт со spec `page-render`: задача останавливается с вопросом пользователю, калибровка и раскладка молча не правятся.
- [ ] 5.4 Прогнать скриншотные тесты и при расхождении переснять эталоны в docker-образе. Проверка — `npm run test:visual` проходит после `npm run test:visual:update`, диф эталонов просмотрен: строки сдвинуты вдоль линий, а не переложены.
- [ ] 5.5 Обновить в `CLAUDE.md` разделы «Как работает генератор» (лист, импорт, отрисовка, зеркалирование) и «Ассеты» (изгиб в `profiles.json`). Проверка — в тексте описаны изгиб, его проверка надёжности, побуквенный путь своего шрифта и то, что раскладка от изгиба не зависит.
- [ ] 5.6 Прогнать полный гейт: `npm run lint`, `npm test`, `npm run test:stories`, `npm run test:visual`, `openspec validate curved-ruling --strict`. Проверка — всё без ошибок и предупреждений в новом коде.
- [ ] 5.7 Приёмка на фото тетради пользователя (клетка на спирали, вне репозитория): переимпорт листа, пачка из нескольких страниц встроенным и своим шрифтом. Проверка — пользователь подтверждает:
  - строки идут по линиям по всей ширине листа, в том числе в углах и на зеркальной странице;
  - текст не заходит за границу сетки.
