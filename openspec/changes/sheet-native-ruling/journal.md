# Журнал прогона

Решения без заказчика, отступления от спеки, итоги аудитов и долг прогона change `sheet-native-ruling`.

## 2026-09-12

### Старт прогона

База прогона: 4bbf8a1136d9483ae93db517c8b58de79f1e9dcd. Масштаб средний (30 задач), режим «дерево», группы последовательно.
Гейт: `npm run lint && npm test`; G8 дополнительно `npm run test:visual` (docker есть).
6.5 вне групп: живая приёмка в браузере со сверкой снимков с фотографией — агент выполнить не может, остаётся [ ] до пользователя.
Решение координатора (K3): спека молчит, должна ли раскладка чётных страниц брать отражённую разлиновку. Выбрано: одна отражённая разлиновка на раскладку и отрисовку. Альтернатива — отражать только при отрисовке. Отвергнута: на наклонном листе вместимость страницы разошлась бы с отрисовкой, приоритет надёжности.
Решение координатора: pre-commit гоняет полный typecheck, поэтому каждая группа оставляет tsc зелёным. План возвращён планировщику: временная совместимость в G1, её удаление — в G8 вместе с 1.4. Альтернатива — красный tsc до G8 — несовместима с коммитом на группу.

### План пересобран

7 групп вместо 8: прежние G2 и G5 (разбивка) слиты в G4 — смена единиц измерителя иначе ломала бы paginate между группами.
Переходный слой (K10): старые поля PaperSheet и сигнатуры остаются с тегом `@deprecated sheet-native-ruling`, снимает G7; проверка — rg по normalizeScale|fitSheetToPage|measuredStep пусто.
У каждой группы критерий «Готово»: typecheck + vitest по её тестам; G3 + build:paper; G7 — полный гейт, test:stories, test:visual.

### G1 · Разлиновка экземпляра

Решения: K1 — resolveFirstLine, K2 — buildSheetRuling + MARGIN_FALLBACK_STEPS (lib/paper/sheetRuling.ts); округление поля до линии с допуском 1e-6 шага; без шага фолбэк полей не считается (ручной ввод). paperSheetJson читает ruling, иначе собирает из старых полей через K2.
Отступления: userSheetsStorage.ts (файл G3) — в toIndexEntry добавлен ruling, иначе tsc; старые поля помечены @deprecated, не удалены (K10). Удалён tests/paginate-sheet-independent.test.ts.
Решение координатора: PaperSheet.firstLinePhase дублирует ruling.firstLinePhase — снимается в G7 вместе с K10. Альтернатива (оставить) отвергнута: два источника фазы разойдутся.
Аудит: ok с первого круга, critical нет. typecheck ok, npm test 441/441.
Долг: (1) paperSheetJson.ts:185 — у новой формы skewAngle/measuredStep берутся с верхнего уровня, а не из ruling — закрыть G3; (2) tests/helpers/paper-family.ts:26 RENDER_SHEET_RULING с нулевыми полями — ловушка для G2/G4: через buildSheetRuling подменятся фолбэком; (3) normalizeSheet.ts, fitSheetToPage.ts без @deprecated на экспортах — G7 найдёт rg по именам; (4) sheetRuling.ts:3-6 комментарий-история (comments.md #5), sheetRuling.ts:27 имя snapDownToLine при ceil — поправить в G7.
Сценарии «разлиновка читается с листа» и «вид разлиновки у семьи» пока только на уровне типов — на итоговый аудит.

### G2 · Калибровка страницы

Решения: deriveGeometry(SheetCalibration, metrics, correction); старая сигнатура — deriveCanonGeometry @deprecated (K10). Линия поля сужает блок, но не выводит за поле (зазор ≥ step/5). K3 — getPageRuling(sheet, pageIndex), единственное ветвление по чётности; getPageCalibration для G4/G6. deriveTextHeight переводит запас снизу в пиксели для вместимости K7. Слайдеры в долях шага: кегль/интервал ±0.25, сдвиги ±2, ширина ±4, запас 0..20.
Отступления: новые селекторы продукт пока не вызывает — подключают G4/G6. Удалён тест «не перекладывает текст при смене экземпляра»: противоречит сценарию «Смена экземпляра пересчитывает геометрию» (буква спеки). На клетке при нечётном запасе или поправке интервала число строк зависит от дробной части — следствие K7.
Аудит: ok с первого круга. typecheck ok, npm test 455/455.
Долг: (1) RealLayout.stories.tsx:139 applyText(LONG_TEXT, 1800) — запас теперь в шагах, поправить в G7 (6.1); (2) usePageGeometry.ts:731 selectPageSheetId внутри useShallow пересчитывается на каждое изменение стора — закрыть в 4.3; (3) usePageGeometry — новые поля и ветка pageIndex без теста хука, добавить при подключении в G4/G6; (4) семья ищется двумя путями (usePageGeometry vs selectActiveFamily); (5) store-geometry-correction.test «разный кегль» зависит от seed рецепта; (6) нет отдельного теста «Смена семьи пересчитывает геометрию» — на итоговый аудит; (7) GeometryGroup: подпись «Высота нижнего поля» при единицах в строках — G7.

### G3 · Профили, конфиг и хранилище листов

Решения: долг G1 закрыт — skewAngle/measuredStep/firstLinePhase берутся из ruling для обеих форм записи. normalizeScale остаётся в записи хранилища @deprecated до G6 (иначе свой лист до G6 рисуется в масштабе 1). Листы без артефакта: шаг клетки ширина/33, линейки высота/25. GRID_RULING, LINED_RULING, CANONICAL_PX_PER_MM помечены @deprecated. try/finally вокруг Chromium в скрипте. Артефакт profiles.json v2, пересобран build:paper.
Отступление: 2.2 требует шаг 52–55 px для всех пресетов; фото линейки дают 70–74 px (клетка 52–55). Решение координатора: тест с границами по семьям (клетка 52–56, линейка 70–75) — буква спеки противоречит данным фото, приоритет надёжности. Вопрос пользователю в финальной пачке, предложена правка 2.2 для /opsx:update.
Удалён тест «канон семьи из констант» — противоречит REMOVED «Семья листов владеет разлиновкой».
Аудит: ok с первого круга; build:paper воспроизводит артефакт байт в байт. npm test 461/461.
Долг: (1) нет теста на отказ сборки на листе без разлиновки (усиление 2.2); (2) paper-preset-families.test.ts:110 min(margins)>0 не отличает найденное поле от фолбэка; (3) у всех 8 листов левое поле = правому, grid-3 верхнее поле 112.7 при сетке от края — поведение detectRuling, на итоговый аудит и 6.5.

### G4 · Разбивка

Решения: splitParagraphs отдаёт end > text.length на разобранном тексте (мнимый \n за последним абзацем — пустой хвост не теряется). Page = {lines}, раскладке LayoutPage = Page & {sheetId} — иначе ломался tsc в файлах G6. sheetId — фактически найденный лист (с фолбэком). paginate кладёт max(1, вместимость || 0) строк — защита от NaN/зацикливания. Ключ кэша сверх K7: кадр листа и family.kind. waitForFont — `1em "<family>"`.
Отступления: usePageGeometry.ts и tests/page-geometry.test.tsx вне списка G4 — по указанию координатора закрыт долг G2 (2) и (3).
Противоречие спеки: сценарий «Низ листа не пустует» не выполним на клетке по формуле K7 (шаг 53, запасные метрики: базовая линия ≥ шага строк от нижнего поля в 92 из 200 высот). Тест проверяет только линейку. Вопрос пользователю в финальной пачке: правка сценария или формулы.
Аудит: ok с первого круга; 2 мутации ключа кэша measureLayout пойманы тестами page-preview. npm test 483/483.
Долг: (1) selectPageSheetSequence без потребителей — подключить в G6 (K5) или убрать; usePageRender (G6) всё ещё зовёт selectPageSheetId — перейти на LayoutPage.sheetId; (2) measure.test.ts:1459 круговая проверка отношения ширин, линейность — story G7; (3) splitParagraphs.ts:588 countParagraphBreaks с начала текста — O(страниц × длина); (4) usePageLayout без семьи/листов даёт EMPTY_PAGES (в продукте недостижимо).

### G5 · Импорт и правка листа в панели

Решения: правка разлиновки сохраняет наклон, marginLineX и marginLineSide листа, поля и шаг берёт из формы через buildSheetRuling. Форма стартует из sheet.ruling; ненайденный шаг — поля пустые, пустой ввод даёт фолбэк K2. RulingForm принимает ruling: SheetRuling. Устаревшие поля заполняются из ruling, normalizeScale — computeNormalizeScale от канона семьи (K10).
Аудит: круг 1 — critical: наклон не проверялся ни одним тестом (импорт skewAngle: 0 и потеря наклона при правке прошли бы). Круг 2 — добавлены тесты импорта листа под 1.2° и сохранения наклона после «Применить разлиновку», зубы проверены мутациями; общие хелперы вынесены в tests/helpers/user-sheet-photo.ts (вне списка файлов G5 в plan.md). Вердикт ok. npm test 492/492.
Долг: (1) сценарий «Разлиновка найдена» — оверлей разлиновки поверх фотографии в продукте не реализован (разрыв базовой спеки, вне 2.5/2.6) — на итоговый аудит; (2) блок заполнения устаревших полей скопирован в PaperGroup.tsx:136 и useSheetImport.ts:140 — G7; (3) useSheetImport.ts:133 при недекодированной фотографии берёт family.width/height — G7 решает этот путь при удалении размеров семьи; (4) settings-panel.test.tsx:326,360 проверяет normalizeScale — G7.

### G6 · Страница в кадре листа: отрисовка и выгрузка

Решения: адаптер отрисовки получает calibration (getPageCalibration) — иначе круговой импорт buildPageRenderParams ↔ geometrySelectors. Отрисовка и пачка берут лист из LayoutPage.sheetId, без него — buildPageSheetSequence (долг G4 закрыт); selectPageSheetSequence удалён. Страница = кадр листа (K8): фон drawImage(0,0,W,H), подложка только под страницу без фото; из RenderBackground убраны x/y, из задания — placement; renderScale убран из рецепта, масштаб снимка 1. resolveShownPageIndex — одно правило номера показанной страницы для предпросмотра и сохранения.
Отступления: правки вне файлов G6 — три stories (G7: background.x/y → 0, sheetId: ''), recipeSelectors.ts и store-run-recipe.test.ts (G4, по долгу). Добавлен CanonGeometryInput @deprecated (K10).
Аудит: круг 1 — critical: тест памяти пачки без зубов (WeakRef на внутреннем объекте, мутация утечки не ловилась). Круг 2 — WeakRef на обёртке PageBlobSurface, мутация ловится; попутно закрыты долги: сохранение при номере за концом раскладки (тест save-bar) и 5.6 на уменьшение числа страниц. Вердикт ok. npm test 506/506.
Долг: (1) BaselineFit/RasterRuling stories берут размер страницы из family.width/height — G7 (6.1); (2) сценарий «Текст остаётся на бумаге» не проверен на отрисованной странице — итоговый аудит; (3) useMemo над buildPageSheetSequence продублирован в usePageRender и useRunRender; (4) resolveShownPageIndex живёт в модуле параметров отрисовки, а не в навигации.

## 2026-09-14

### G7 blocked

Исполнитель G7: 1.4 6.1 6.3 6.4 сделаны, 6.2 красный — test:visual 68/69.
Причина: K4 на Linux Chromium не держится. Ширины там целые, и расхождение с «доля кегля × кегль» доходит до 20 px (0,9 %) при кегле 61 px; на macOS ≤0,012 px. Из-за этого story WidthScalesWithFontSize красная в docker.
Вопрос пользователю: запас к ширине блока + относительный допуск / замер на кегле страницы / только допуск.
Ещё два открытых вопроса пользователю: диапазон шага в 2.2 и сценарий «Низ листа не пустует» на клетке.
Решения G7: фаза листов без артефакта 0; неразобранная фотография получает кадр первого листа семьи; snapDownToLine → ceilToLine; подпись «Запас снизу в шагах разлиновки».
Ключ measuredStep остался только в разборе записи прежней формы (нужен для 2.4).

### Ответы пользователя

2.2, шаг пресетов: правится цифра в задаче — клетка 52–56 px, линейка 70–75 px. Код не меняется. Правка артефакта предложена в итоге для /opsx:update.
«Низ листа не пустует» на клетке: строку добирать. Последняя строка ставится, если её базовая линия помещается над нижним полем; хвосты букв могут зайти в поле. Меняется формула вместимости (K7) — раскладка и отрисовка.
K4 на Linux Chromium: к ширине блока закладывается запас ~1%, в story WidthScalesWithFontSize — относительный допуск 1%.
План: сначала аудит G7 без 6.2, потом группа G8 — добор строки, запас 1%, переснятие эталонов и зелёный test:visual, закрывает 6.2. Если аудит G7 вернёт critical, решения идут в тот же круг.

### G7 · Снятие переходного слоя, stories, документация и гейт

Решения: переходный слой K10 снят — удалены normalizeSheet/fitSheetToPage с тестами, старые поля листа и семьи, PaperSheet.firstLinePhase. Фаза листов без артефакта 0; неразобранная фотография получает кадр первого листа семьи. snapDownToLine → ceilToLine, комментарий sheetRuling.ts без истории. Подпись слайдера «Запас снизу в шагах разлиновки». Stories: WidthScalesWithFontSize (K4), PhotoRulingOnSheet вместо PhotoRulingMatchesCanon; CLAUDE.md переписан под разлиновку экземпляра.
Решения пользователя (круг 1): WRAP_WIDTH_SLACK = 0.01 — запас только к ширине переноса строк, края блока не меняются; допуск 1 % в story линейности. Вместимость добирает строку: последняя ставится, пока базовая линия не ниже нижнего поля (countPageLines) — линейка 9 строк вместо 10 по числам кадра, клетка без пустого ряда внизу.
Отступление: ключ measuredStep остался в разборе записи прежней формы (paperSheetJson.ts) — нужен для 2.4.
Аудит: круг 1 — critical: эталон Even-Page переснят с неотражённой страницей (обвязка давала одну страницу). Круг 2 — две страницы, линия поля слева; мутации запаса и формулы пойманы. Вердикт ok. npm test 476/476, test:stories 69, test:visual 69.
Долг: (1) K7 в plan.md и SHALL в page-calibration описывают прежнюю формулу вместимости — /opsx:update; (2) допуск 1 % при расхождении до 0,9 % — story может мигать; (3) формула вместимости повторена в store-geometry-correction и paper-profiles тестах; (4) useSheetImport.ts:131 без теста; (5) на Even-Page верх первой строки срезан краем кадра — детектор даёт верхнее поле ≈40 px на клетке (долг G3) — на итоговый аудит.

### Итоговый аудит — круг 1

Финальный гейт после G7 зелёный (lint, test, test:stories, test:visual), openspec validate --strict ok. Итоговый аудит вернул critical, открыта группа F1:
(1) detectRuling не отдаёт 0 для ненайденного поля: левое и правое = guard comb-профиля, верхнее и нижнее = крайние видимые линии. Фолбэк K2 не срабатывает ни на одном пресете: у grid-1 и grid-2 верх строчного бокса за кадром (−12…−45 px) — это и есть срезанная строка Even-Page; у grid-3 блок в 5 px от края.
(2) «Текст остаётся на бумаге» не проверен на отрисованной странице — нужна растровая проверка на пресетах, на нечётной и зеркальной странице.
Долг в F1: тест «Смена семьи пересчитывает геометрию».
Покрытие: требований 14, сценариев 40, без проверки 3.
Правки для /opsx:update:
- page-calibration spec.md:16 и K7 — формула вместимости с добором строки;
- tasks.md 2.2 и plan.md G3 — шаг 52–56 / 70–75 px;
- K4 — WRAP_WIDTH_SLACK 1 %;
- design.md:157 — ложное утверждение про нули детектора (проверить после F1);
- paper-profile «Разлиновка найдена» — оверлей унаследован из базовой спеки: убрать из MODIFIED или вынести в отдельный change;
- «Текст остаётся на бумаге» — добавить условие, что верх первой строки и низ последней не выходят за кадр.
Долг вне F1:
- renderPageToCanvas.ts:413 молча выбрасывает слово за краем блока;
- getPageRuling, selectBlockSkewAngle и MARGIN_FALLBACK_STEPS снаружи используются только тестами;
- допуск 1 % при расхождении до 0,9 %;
- формула вместимости повторена в тестах;
- useSheetImport.ts:131 без теста.

### F1 — спор исполнителя

Исполнитель F1 вернул disputed по двум пунктам:
(1) Левая граница в TextStaysOnPaper: чернила ≥ левый край блока − свес первых глифов − 1 px, а не ≥ margins.left.
(2) На зеркальной странице (grid-2, grid-4, lined-1, lined-3) хвост «д» Abram (левый отступ −0,156 em) заходит за линию поля на 1–1,6 px.
Решение координатора: принять вариант (в) — допуск на свес глифа в story; раскладка и отрисовка не меняются.
Обоснование: спека «Текст остаётся на бумаге» и зазор step/5 говорят о краях блока, их блок выполняет; про чернила глифа спека молчит. Свес задан самим шрифтом, в тетради хвост буквы на линии поля естественен.
Отвергнуто: (а) сдвиг начала строки на свес первого глифа — меняет K4/K7 и эталоны ради 1–1,6 px; (б) шире зазор до линии поля — меняет число в спеке.
Правка для /opsx:update: уточнить в «Текст остаётся на бумаге», что граница — край блока, свес глифа допустим.
Профили пресетов после F1: у клетки все поля, кроме верхнего grid-3, взяты фолбэком (разлиновка до края); у линейки фолбэком взяты бока, верх и низ найдены.

### F1 — аудит, круг 1

Поправка к записи «F1 — спор исполнителя». Утверждение «у линейки верх и низ найдены» неверно: у lined-1, lined-2 и lined-4 разлиновка на фото доходит до нижнего края кадра, а detectRuling отдаёт найденное нижнее поле 157,8, 214 и 209 px.
Причина: последние линии сдвинуты на −6…−9 px от гребёнки, окно глубины ±1 бин их не видит, и цепочка рвётся раньше конца профиля. Верх найден верно.
Critical ушёл исполнителю F1 (круг 1 из 2): локальный поиск глубины вокруг предсказанной линии, тест со сдвинутыми линиями, пересборка build:paper.
Долг в тот же круг:
- topOffset ≥ 0 проверять и с метриками шрифта по умолчанию;
- построчная левая граница с учётом свеса в TextStaysOnPaper;
- импорт константы зазора до линии поля вместо числа 0.2.
Решение координатора по свесу глифа аудитор подтвердил: свес считается из контуров шрифта, правая граница строгая.

### F1

F1 — доработка по итоговому аудиту.
Решения: detectRuling отдаёт 0 для стороны, где разлиновка доходит до края профиля; глубина линии в findRuledSpan — максимум в окне ±round(шаг/6), так последние линии, ушедшие с гребёнки на −6…−9 px, не рвут цепочку. Профили пересобраны: у клетки все поля фолбэком (верх grid-3 тоже), у линейки фолбэком бока и низ, верх найден. Story TextStaysOnPaper проверяет на растре все пресеты, нечётную и зеркальную страницу: правая граница строгая, левая построчная со свесом первой буквы из контура, верх на метриках шрифта. Тест «Смена семьи пересчитывает геометрию». MARGIN_LINE_GAP_SHARE экспортирован из lib/calibrate.
Решение координатора (спор): свес глифа за краем блока и хвост «д» за линией поля на 1–1,6 px допустимы — спека говорит о краях блока.
Аудит: круг 1 — critical: нижнее поле lined-1/2/4 «найдено» при линейке до края (окно ±1 бин). Круг 2 — окно шаг/6, lined-1…4 низ фолбэком, тест со сдвинутыми линиями, мутация окна поймана. Вердикт ok. npm test 482, test:stories 71, test:visual 71; 14 эталонов пересняты сменой полей.
Долг: isInsideOverhang без правого края габарита; MARGIN_LINE_GAP_SHARE снаружи только в stories; detectRuling.ts:58 комментарий «восьмая шага» при фактическом 1/12–1/8.
