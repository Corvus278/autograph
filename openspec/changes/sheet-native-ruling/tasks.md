## 1. Модель листа

- [x] 1.1 Добавить тип `SheetRuling` (шаг, фаза, наклон, поля, линия поля со стороной) и поле `ruling` в `PaperSheet`; убрать из `PaperSheet` `measuredStep`, `normalizeScale` и `skewAngle`, из `PaperFamily` — `width`, `height` и `ruling`, оставив `kind`; проверка — `npm run typecheck` показывает все места, которые предстоит перевести
- [x] 1.2 Написать `mirrorSheetRuling(ruling, width)`: отражение `marginLineX`, смена стороны линии поля, инверсия угла, обмен левого и правого полей и сдвиг фазы с верхним и нижним полями на `tan(угол) × ширина кадра`; проверка — юнит-тест на наклонном листе: первая базовая строка зеркальной страницы попадает на линию отражённой разлиновки с допуском в одну десятую шага (двойным отражением не проверять — симметричное слагаемое сокращается)
- [x] 1.3 Написать сборку разлиновки из результата детектора с фолбэком по каждой стороне отдельно: ненайденное поле — полтора шага от края кадра, верхнее округляется вверх до линии, линия поля `null`; проверка — юнит-тесты на детекцию без полей, на детекцию с полями и стороной и на попадание первой строки на линию при фолбэке
- [ ] 1.4 Удалить `lib/paper/normalizeSheet.ts` и `lib/paper/fitSheetToPage.ts` вместе с их тестами; проверка — `rg "normalizeScale|fitSheetToPage" src scripts tests` ничего не находит

## 2. Профили, конфиг и хранилище

- [x] 2.1 Перевести `scripts/build-paper-profiles.ts` на новую форму профиля: в экземпляр пишется `ruling` целиком, фотография не нормируется, ненайденная разлиновка валит сборку с указанием файла; проверка — `npm run build:paper` проходит и печатает найденные шаг, поля и линию поля по каждому листу
- [x] 2.2 Пересобрать `public/paper/profiles.json` и сверить напечатанные величины с фотографиями (линия поля пресет-пака — у правого края, шаг 52–55 px); проверка — значения в артефакте совпадают с напечатанными, версия артефакта поднята
- [x] 2.3 Перевести `config/paperFamilies.ts` и `config/config.types.ts`: семья несёт `kind` без размеров и канона, фолбэк-листы `buildPlainSheets` получают синтезированную разлиновку (шаг долей кадра из прежних канонов, поля фолбэком); проверка — юнит-тест: до загрузки артефакта семья даёт листы с ненулевым шагом и рисуемой геометрией
- [x] 2.4 Перевести `model/paperSheetJson.ts` и `model/userSheetsStorage.ts` на новую форму с конвертацией записей прежней формы (шаг и фаза из сохранённых, поля фолбэком); проверка — юнит-тесты на чтение записи прежней формы (лист остаётся в списке с приблизительной разлиновкой) и записи новой формы
- [ ] 2.5 Перевести `useSheetImport` на сохранение полной разлиновки, включая поля и линию поля; проверка — компонентный тест импорта: у добавленного листа есть `ruling` с найденными величинами
- [ ] 2.6 Перевести `RulingForm` и `PaperGroup` на правку полной разлиновки: исправленные поля сохраняются в пользовательской копии листа наравне с шагом и фазой; проверка — компонентный тест: после правки полей блок текста выложен по исправленным величинам

## 3. Калибровка

- [x] 3.1 Перевести `deriveGeometry` на `SheetRuling` + `RulingKind`: верхний отступ от первой линии не выше верхнего поля, левый край и ширина блока из стороны линии поля; проверка — юнит-тесты на четыре случая: линия слева, линия справа, линии нет, поле не кратно фазе
- [x] 3.2 Перевести `usePageGeometry` и `geometrySelectors` на геометрию конкретной страницы: разлиновка и наклон берутся из листа этой страницы, а не из активного экземпляра; проверка — юнит-тест селектора на двух страницах с разными листами
- [x] 3.3 Перевести `GeometryCorrection` и `bottomMargin` на доли шага разлиновки, пересчитать диапазоны слайдеров; проверка — юнит-тест «одна и та же поправка на листах с разным шагом даёт один и тот же сдвиг в долях шага», тест «запас снизу отнимает одинаковое число строк» и компонентный тест панели

## 4. Разбивка

- [x] 4.1 Перевести `DomMeasurer` на измерение долями кегля, убрать `measureLineHeight` из `TextMeasurer`, сохранив его в `FontMetricsProbe`; проверка — юнит-тест: ширина строки при двух кеглях отличается ровно в отношении кеглей с субпиксельной точностью, `measureFontMetrics` продолжает работать
- [x] 4.2 Научить `splitParagraphs` продолжать с заданной позиции и сообщать, где остановился; проверка — юнит-тест на продолжение с середины абзаца
- [x] 4.3 Построить последовательность листов прогона инкрементально, без пересборки рецепта на каждой странице; проверка — юнит-тест: последовательность на `n` страниц совпадает с поэлементными вызовами `selectPageSheetId`, число пересборок рецепта не растёт квадратично
- [x] 4.4 Заменить `paginate` последовательной раскладкой: страница набирается под разлиновку своего листа, вместимость считается по `lineStep` модели строчного бокса; проверка — юнит-тесты на перетекание без потерь (склейка строк даёт исходный текст), на разное число строк у листов с разным шагом и на слово шире блока
- [x] 4.5 Перевести `measureLayout` и `usePageLayout` на последовательную раскладку и полный ключ кэша (текст, шрифт, метрики, поправка, запас снизу, seed прогона, семья, отпечаток списка листов, закреплённый лист); проверка — компонентные тесты: повторная раскладка с теми же параметрами не измеряет заново; добавление своего листа в семью раскладывает заново
- [x] 4.6 Удалить тест `tests/paginate-sheet-independent.test.ts` и перевести `tests/helpers/paper-family.ts` на новую модель; проверка — `npm test` проходит

## 5. Отрисовка и выгрузка

- [ ] 5.1 Перевести `buildPageRenderParams` на разлиновку листа страницы и `mirrorSheetRuling`, убрать `mirrorLeftPadding` и `mirrorSkewAngle`; проверка — юнит-тест: на зеркальной странице блок отступает от линии поля на ту же величину, что и на нечётной
- [ ] 5.2 Сделать размер страницы равным кадру листа, в том числе при скрытом фоне (`usePageRender`, `buildPageTask`, `renderPageRequest`, `createPageRenderClient`); проверка — тест: размеры страницы равны кадру листа, скрытие фона их не меняет
- [ ] 5.3 Перевести растеризацию на разрешение кадра: масштаб становится величиной страницы, а не прогона, множитель разрешения уходит из `PageOpticsRecipe` (`useRunRender`, `useExportPage`, `useBatchExport`, `lib/recipe`); проверка — тест: ширина и высота снимка равны кадру листа этой страницы
- [ ] 5.4 Убрать отрисовку фона прямоугольником укладки — фон рисуется во всю страницу; проверка — тест отрисовки: фон занимает всю канву, подложки не остаётся
- [ ] 5.5 Проверить пачку на страницах разного размера: архив собирается, имена и порядок прежние, память не растёт; проверка — юнит-тест пачки на двух листах с разными кадрами
- [ ] 5.6 Проверить предпросмотр при листах с разными пропорциями: ширина задана контейнером, высота идёт за кадром, `clampPageIndex` не выбрасывает пользователя со страницы при пересчёте числа страниц; проверка — компонентный тест смены страницы на листах с разными пропорциями

## 6. Тесты, stories и документация

- [ ] 6.1 Перевести stories `BaselineFit`, `RasterRuling` и `RealLayout` на разлиновку листа; проверка — `npm run test:stories` проходит, базовые линии попадают на линии разлиновки, включая зеркальную страницу наклонного листа
- [ ] 6.2 Прогнать скриншотные тесты и переснять эталоны: размер страницы и кегль меняются на всех листах; проверка — `npm run test:visual` проходит после `npm run test:visual:update`
- [ ] 6.3 Обновить разделы «Как работает генератор» и «Ассеты» в `CLAUDE.md` под новую модель; проверка — в тексте нет упоминаний канона семьи и нормировки
- [ ] 6.4 Прогнать полный гейт: `npm run lint` и `npm test` без ошибок и предупреждений
- [ ] 6.5 Сгенерировать пачку из пяти страниц на пресет-паке и замерить результат: лист покрывает страницу целиком, текст не выходит за поля и не ложится на линию поля, низ листа заполнен строками, базовые линии сидят на разлиновке и на зеркальных страницах; проверка — замер выгруженных файлов подтверждает все четыре условия
