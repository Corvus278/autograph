## 1. Иконки приложения

- [x] 1.1 Написать `scripts/build-app-icons.ts` (esbuild → node → Chromium из Playwright, по образцу `scripts/build-paper-profiles.ts`): растеризует `public/favicon.svg` в `public/icon-192.png` и `public/icon-512.png`; проверка — `npm run build:icons` создаёт оба файла нужных размеров
- [x] 1.2 Добавить в тот же скрипт `maskable`-вариант `public/icon-maskable-512.png`: рисунок вписан в центральные 80 % холста, фон во весь квадрат; проверка — файл создан, рисунок не касается краёв
- [x] 1.3 Завести скрипт `build:icons` в `package.json` и закоммитить сгенерированные PNG; проверка — `npm run build:icons` на чистом дереве не меняет файлы

## 2. Сборка: манифест и service worker

- [x] 2.1 Поставить `vite-plugin-pwa` в devDependencies; проверка — `npm ci` проходит, `npm run build` не падает
- [x] 2.2 Подключить плагин в `vite.config.ts` с `registerType: 'prompt'`, манифестом (имя, короткое имя, описание, `display: 'standalone'`, цвета темы и фона, три иконки из группы 1) и `devOptions` выключенными; проверка — `npm run build` кладёт в `dist` `manifest.webmanifest` и `sw.js`
- [x] 2.3 Задать `workbox.globPatterns` со всеми расширениями `dist` (`js,css,html,json,svg,png,jpg,ttf,webmanifest`), `cleanupOutdatedCaches: true`, `navigateFallback: 'index.html'`; проверка — precache-манифест в `dist/sw.js` содержит все восемь листов, их текстуры, все `.ttf` и `profiles.json`
- [x] 2.4 Добавить в `index.html` мета-теги темы (`theme-color`); проверка — `npm run build`, в `dist/index.html` есть ссылка на манифест (её вставляет плагин) и мета-тег темы
- [x] 2.5 Проверить сборку с базовым путём: `BASE_PATH=/autograph/ npm run build` — `scope`, `start_url`, адреса precache и `navigateFallback` начинаются с `/autograph/`; проверка — прогон `npm run preview` по этому пути открывает приложение

## 3. Проверка полноты precache

- [x] 3.1 Написать тест `tests/pwa-precache.test.ts`: собирает проект (или читает уже собранный `dist`, если он есть), сверяет precache-манифест с `find dist -type f` за вычетом `sw.js`, workbox-рантайма и `404.html`; расхождение — падение теста; проверка — тест зелёный на текущем наборе ассетов
- [x] 3.2 Дополнить тест проверкой размера: файл в `dist` крупнее `maximumFileSizeToCacheInBytes` обязан попасть в манифест либо уронить тест; проверка — искусственное превышение порога в тесте роняет его

## 4. Регистрация service worker и состояние

- [x] 4.1 Добавить `vite-plugin-pwa/client` в `types` у `tsconfig.json`; проверка — `npm run typecheck` видит модуль `virtual:pwa-register`
- [x] 4.2 Написать `src/app/model/serviceWorkerRegistration.ts` — единственный файл с импортом `virtual:pwa-register`: вызывает `registerSW` с `immediate: true`, `updateViaCache: 'none'`, пробрасывает колбэки наружу, дёргает `registration.update()` по часовому таймеру и на возврат вкладки в фокус; проверка — `npm run lint` чистый, файл не импортируется никем, кроме хука и точки входа
- [x] 4.3 Написать `src/app/model/useServiceWorkerState.ts` — хук с состоянием `isOfflineReady` / `hasUpdate` и действиями «обновить» и «закрыть сообщение»; функция регистрации приходит аргументом с дефолтом из 4.2; проверка — юнит-тест `tests/service-worker-state.test.ts` на все переходы, включая отказ регистрации
- [x] 4.4 Вызвать регистрацию из `src/app/main.tsx` под `import.meta.env.PROD`; проверка — `npm run dev` и `npm run storybook` не регистрируют service worker (в DevTools → Application пусто)

## 5. Плашка обновления и сообщение о готовности

- [x] 5.1 Написать `src/app/ui/ServiceWorkerBanner/` — плашка с кнопкой «Обновить» и сообщением «Приложение готово к работе без сети»; вёрстка на токенах темы, не перекрывает область просмотра листа, закрывается по действию пользователя, сообщение о готовности само гаснет; проверка — `npm run lint` чистый
- [x] 5.2 Отрендерить плашку в `src/app/App.tsx` поверх маршрутов; проверка — шапка (`AppHeader`) не изменена, её скриншотные эталоны не переснимаются
- [x] 5.3 Компонентный тест `tests/service-worker-banner.test.tsx` (jsdom): появление плашки при доступном обновлении, вызов обновления по кнопке, отсутствие плашки без обновления, разовость сообщения о готовности; проверка — `npm test` зелёный
- [x] 5.4 Прогнать `npm run lint:fsd`; если steiger не принимает сегменты `ui`/`model` в слое `app`, перенести плашку и состояние в `widgets/ServiceWorkerBanner` без изменения контракта компонента; проверка — `npm run lint` чистый

## 6. Приёмка оффлайна

- [ ] 6.1 Прогнать сценарии spec на `npm run preview` с выключенной сетью: заход на `/`, прямой заход на `/create-font`, неизвестный адрес, выбор листа и почерка, не открывавшихся в сети, сохранение страницы, выгрузка пачки; проверка — все семь сценариев проходят без сети
- [ ] 6.2 Проверить установку приложения из браузера и совпадение сессии между вкладкой и установленным окном; проверка — приложение открывается отдельным окном, текст и настройки те же
- [ ] 6.3 Проверить цикл обновления: собрать вторую версию, подменить раздачу при открытом приложении, дождаться плашки, нажать «Обновить»; проверка — приложение на новой версии, набранный текст сохранён, кэш прежней версии удалён
- [ ] 6.4 Прогнать `npm run lint` и `npm test` целиком; проверка — 0 errors, 0 warnings, все тесты зелёные

## 7. Документация

- [ ] 7.1 Добавить в `CLAUDE.md` раздел про PWA: состав precache, где живёт регистрация и плашка, почему dev без service worker, как пересобрать иконки, аварийная разрегистрация при ошибочном релизе; проверка — раздел описывает все пять пунктов
- [ ] 7.2 Отразить в `CLAUDE.md` новые команды и файлы (`build:icons`, `public/icon-*.png`, тест полноты precache) в разделах «Стек и сборка», «Структура», «Ассеты» и «Тесты»; проверка — перечисленное в документации совпадает с деревом репозитория
