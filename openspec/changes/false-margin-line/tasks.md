## 1. Опора для замеров

- [x] 1.1 Убедиться, что работа идёт в ветке `fix/10-false-margin-line` от свежего `master`. Проверка: `git log --oneline master..HEAD` показывает только коммиты этой задачи.
- [x] 1.2 Скопировать скрипт эталона `/tmp/margin-line-trace/ground-truth-script.ts` в `/tmp/false-margin-line/` (если его нет — воссоздать по решению 6 из `design.md`). Проверка: скрипт на `IMG_1806` печатает самое внутреннее `u` 2425,0 ± 1 px и цвет столбца с `R > G`, как в `/tmp/margin-line-trace/ground-truth.md`.
- [x] 1.3 Снять опору по всем 26 снимкам `paper_photos_examples/` и восьми пресетам: `npm run measure:sheet` по каждому, вывод в `/tmp/false-margin-line/baseline.md`. Проверка: строки линии поля совпадают со списком из `proposal.md` (одиннадцать настоящих линий и два фантома: `IMG_1813` 1640,7 right, `IMG_1803` 111,7 left), у пресетов — с `marginLineX` в `public/paper/profiles.json`; `md5 -q public/paper/profiles.json` печатает `bbb9e9b963bc092e50aad90f60fd39ce`.
- [ ] 1.4 Снять время опоры медианой трёх прогонов на снимок для всех 26 и восьми пресетов (бандл с базы ветки, прогоны вперемешку). Проверка: таблица «снимок → три замера → медиана» лежит в `/tmp/false-margin-line/baseline.md`.
- [x] 1.5 До любых правок кода разметить цветом все 26 снимков скриптом из 1.2 (решение 6): по каждому — класс «черта есть / нет», сторона и самое внутреннее положение (`x` и `u`), ΔR и ΔG по полосам. Это опора неухудшения вместо ответов детектора (урок #9: `IMG_1813` и `IMG_1803` записаны в класс «линия есть» по ответу детектора — ошибались метки, а не не хватало невиденных снимков). Проверка: таблица «снимок → класс, сторона, `u`, ΔR/ΔG» в `/tmp/false-margin-line/ground-truth.md`; в ней `IMG_1813` — черта left, самое внутреннее `u` 401 (ΔR 41,0 против ΔG 60,8), столбец фантома 1645 нейтральный (36,0 против 24,1); `IMG_1803` — черта left на `u` 591…638 (ΔR 30 против ΔG 66), столбец 111,7 — нейтральная тень (`R < G`); `IMG_1812` — черта left около 473,9. Расхождение с этими числами аудитора — стоп и вопрос координатору.

## 2. Диагноз первой ступени

Диагноз аудитора — Д4 и Д1 (`design.md`, решение 1). Группа воспроизводит его числа и снимает опору десяти настоящих линий; исходники не меняются.

- [x] 2.1 Напечатать для `IMG_1813` и `IMG_1812` числа первой ступени по решению 1, пункт 1: по три пика в каждой крайней трети (`x`, глубина, отстояние от фазы клетки, расстояние до границы трети), дециль соседей, `6σ`, минимум, барьер и связавший порог, отношения к барьеру; глубину вертикалей клетки по ширине; глубину профиля в столбце эталона левой черты из 1.5. Инструментованной копией бандла или скриптом. Проверка: таблица в `/tmp/false-margin-line/diagnosis.md` воспроизводит числа аудитора на `IMG_1813` — треть с 1633, фантом 1645, глубина 0,061, отношение 1,26 при барьере 0,048 (связали соседи), вертикали 1404…1802 с глубинами 0,050 / 0,082 / 0,081 / 0,061 / 0,040 / 0,029, лучший левый кандидат 0,13 барьера; `git status` не показывает правок в `src/`.
- [x] 2.2 Напечатать для `IMG_1813`, `IMG_1812` и `IMG_1803` результат полосовой ступени при принудительной стороне `left` и `right` (пункт 2): охват, медиана глубины, отношение к полосовой децили, связавший порог — по проходу по кадру и по копии. Проверка: четыре строки на снимок в `diagnosis.md`; на `IMG_1813` справа 0,90 / 0,99, слева 2,94 / 2,90 с линией 396,0 px left.
- [x] 2.3 Проверить краевой эффект окна (пункт 4): ширина вырезки, начало правой трети, окно фона у фантома, глубина фантома `IMG_1813` при сдвиге границы трети на +0,5 и −1 шаг. Проверка: в `diagnosis.md` числом записано, что окно фона не обрезано, при +0,5 шага фантома нет, при −1 шаге он остаётся.
- [x] 2.4 Снять числа первой ступени и полосовой меры для всех десяти настоящих линий, которые сейчас берёт первая ступень: восемь пресетов, `IMG_1802`, `IMG_1804` (пункт 5). `IMG_1803` сюда не входит — его линия первой ступени фантом. Проверка: таблица «линия → отношение по полосовой мере, связавший порог, охват» в `diagnosis.md`; у пресетов — отношения 3,35…3,86 (клетка), глубина 0,21 при этаже 0,03 (линейка).
- [x] 2.5 Записать диагноз и проверить условие применимости вето (решение 2): фантом `IMG_1813` не проходит полосовую меру с запасом не меньше 0,1 до барьера, все десять линий из 2.4 проходят с тем же запасом. Проверка: в журнале прогона записаны диагноз (Д4 + Д1), механизм «вето» и числа условия. Если условие не выполнено — стоп и вопрос координатору; если нужен цвет (`SheetImageData` в RGB) — стоп и вопрос пользователю.

## 3. Синтетические листы

- [x] 3.1 Добавить в `tests/helpers/synthetic-sheet.ts` фиксированный лист в клетку, воспроизводящий **причину** картины `IMG_1813`: схождение клетки (перспектива при постоянном сдвиге столбцов профиля) без вставной вертикали, черта у левой стороны со сносом по высоте. Проверка: юнит-тест на базе ветки фиксирует, что глубина вертикалей клетки в профиле во всю высоту неоднородна по ширине и что нынешний детектор отдаёт линию у правой стороны — дефект воспроизведён причиной. Если причиной не воспроизводится — стоп и вопрос координатору, вставную вертикаль молча не подставлять.
- [x] 3.2 Добавить параметрический генератор удержанной выборки по решению 5 (сторона черты, снос, схождение клетки, отношение вставной ложной вертикали к соседям — включая нулевое, её положение, градиент света, пятно), класс по построению. Сами листы не разыгрывать. Проверка: юнит-тест генератора при фиксированных параметрах даёт растр бит-в-бит одинаковый между вызовами, а вставная вертикаль имеет заданное отношение глубины к соседним вертикалям клетки с точностью до 5 %. Отношение меряется в самом тесте напрямую по растру — средняя яркость столбца вертикали против бумаги рядом, делённая на то же у соседних вертикалей клетки, — без `findMarginLine`, `findBandedMarginLine` и `toMarginLineBarrier`: мерка детектора не может проверять вход, по которому проверяют детектор.

## 4. Механизм

- [ ] 4.1 Реализовать в `src/pages/Generator/lib/paper/detectRuling.ts` вето полосовой мерой на кандидате первой ступени (решение 2) внутри измерения — до `refineMarginLine` и до обрезки области изгиба (решение 4), на обоих проходах. `toMarginLineBarrier` и константы `MARGIN_LINE_*` не менять (решение 3). Проверка: юнит-тест на листе из 3.1 — линия поля не у правой стороны: либо `null`, либо слева в пределах 0,2 шага от самого внутреннего положения черты; `rg -n "MARGIN_LINE_DEPTH_RATIO = 1.5" src` находит прежнее определение.
- [ ] 4.2 Вето сохраняет все правила полосовой меры #9: трассу, медиану, охват, барьер и правило края полосы (`isInsideMeasuredBins`: кандидат ближе полуокна фона к краю полосы не берётся). Проверка: юнит-тест с кандидатом первой ступени ближе полуокна фона к краю профиля (картина копии `IMG_1803`: 28 бинов при полуокне 62) — вето его отвергает.
- [ ] 4.3 Выбор стороны в полосовой ступени только среди принятых кандидатов: при незаданной стороне `findBandedMarginLine` сравнивает только кандидатов, прошедших барьер, а не `left.depth >= right.depth` до барьера. Проверка: юнит-тест на синтетическом листе, где у одной стороны кандидат глубже, но ниже барьера, а у другой — мельче, но выше барьера: возвращается второй; лист, где барьер не прошёл ни один, — `null`.
- [ ] 4.4 Сторожить ровный путь. Проверка: юнит-тест на синтетическом ровном листе с прямой линией поля, которую сейчас берёт первая ступень, — `marginLineX` и сторона совпадают с прежними до бита; тест с `marginLineSide: null` — линия не ищется ни одной ступенью.
- [ ] 4.5 Сторожить отрицательный класс. Проверка: юнит-тесты на листах без черты из `tests/helpers/synthetic-sheet.ts` (клетка без черты, клетка со схождением без черты, лист с глубокой вертикалью на фазе) — `marginLineX` равен `null`. Листы без канала `R − G`: отрицательный класс держат яркостные меры, вето по цвету здесь молчит.
- [ ] 4.6 Сторожить сетку изгиба: на листе из 3.1 граница области изгиба со стороны снятой линии больше не проходит по ней. Проверка: юнит-тест сверяет границу сетки с записанной в разлиновку линией поля (или с полем листа, если линии нет).
- [ ] 4.7 Повторяемость. Проверка: два измерения листа из 3.1 совпадают через `toStrictEqual`.

Задачи 4.8—4.12 — вето по цвету (решение 2, «Вето по цвету»; журнал, «Решение координатора: цвет как вето»).

- [ ] 4.8 Канал `R − G`: необязательное поле `Int16Array` в `SheetImageData` (`paper.types.ts`, построчно, как `luminance`), заполнение в `decodeSheetImage` (`ui/Generator/SettingsPane/useSheetImport/`) и в `decodeSheetPhoto` (`scripts/sheet-photo-report.ts`), перенос тем же отображением пикселей, что у яркости, в вырезку (`cropSheet` в `measureSheetPhoto.ts`) и в выпрямленную копию (`rectifySheetImage` в `measureSheetPhotoRectify.ts`). Там, где канала нет на входе, его нет и в вырезке и копии. Проверка: юнит-тест на синтетическом изображении, где `R − G` поэлементно задан функцией от яркости, — после `cropSheet` и `rectifySheetImage` эта связь держится в каждом пикселе; без канала на входе вырезка и копия без поля; скрипт вне репозитория, декодирующий `IMG_1808` через `decodeSheetPhoto`, печатает P99 `|R − G|` 27 ± 1 (число диагноста), а для `preset-grid-1` — 17 ± 1; `npm run typecheck` зелёный.
- [ ] 4.9 Мера красноты кандидата и гейт серого снимка. Краснота — медиана по узлам трассы кандидата избытка `R − G` в его столбце над скользящей медианой `R − G` по строке (окно фона яркостного профиля), в уровнях. Гейт — P99 `|R − G|` по кадру: меньше 5 или канала нет — вето по цвету выключено. Константы — `MARGIN_LINE_MIN_REDNESS = 16` и порог гейта 5, с обоснованием из решения 2 в JSDoc; `toMarginLineBarrier` и `MARGIN_LINE_*` из решения 3 не меняются. Проверка: юнит-тесты на листах из 4.11 — у красной черты с избытком 30 уровней краснота 30 ± 3, у нейтральной вертикали той же яркостной глубины — не больше 2; гейт выключен при P99 4 и без канала, включён при P99 6; `rg -n "MARGIN_LINE_DEPTH_RATIO = 1.5" src` находит прежнее определение.
- [ ] 4.10 Вето по цвету в обеих ступенях, на обоих проходах, до `refineMarginLine` и до обрезки области изгиба. Первая ступень: кандидат принимается, только если прошёл вето по стороне (4.1) и красноту. Вторая: при незаданной стороне — самый глубокий среди прошедших барьер и красноту с обеих сторон, при заданной — у этой стороны; нет годных — `null`. `x` принятого — от яркостной меры до бита. Проверка: юнит-тесты на цветных листах из 4.11 — (а) нейтральная вертикаль (виток спирали, линия клетки, тень) глубже барьера у одной стороны и красная черта у другой — линия у черты в пределах 0,2 шага от самого внутреннего положения; (б) только нейтральная вертикаль глубже барьера — `null`; (в) красная черта, которую находит яркостная мера, — `marginLineX` и сторона те же, что у того же листа без канала, до бита; (г) тот же лист без канала и с серым гейтом — результат через `toStrictEqual` равен яркостному пути; тесты (а) и (б) падают, если убрать вето по цвету из любой одной ступени (мутация проверена и записана в журнал).
- [ ] 4.11 Синтетика с цветом: в `tests/helpers/synthetic-sheet.ts` — канал `R − G` и цвет черты и вертикалей (красная черта, бледная красная черта, нейтральная вертикаль той же яркостной глубины, виток спирали на листе в линейку, нейтральная тень); у генератора удержанной выборки из 3.2 — цвет черты и вставной ложной вертикали, класс по построению. Без новых полей растр прежний. Проверка: юнит-тест генератора — без полей цвета яркость до бита совпадает с прежней (хэш массива), канала нет; с цветом избыток `R − G` на черте против бумаги рядом равен заданному с точностью 5 %, меряется по растру в тесте, без мер детектора.
- [ ] 4.12 Удешевление: полосы красноты считаются только в крайних третях ширины и только у стороны, где есть кандидат выше барьера; иначе канал не читается. Проверка: итог измерения тем же бандлом с удешевлением и без него совпадает до бита по строкам `measure:sheet` всех 26 снимков и 8 пресетов; медиана трёх прогонов времени вето по цвету на снимок не больше 1,6 с и не больше, чем без удешевления; таблица «снимок → без цвета / с цветом / с удешевлением» в `/tmp/false-margin-line/timing-colour.md`.

## 5. Диагностика и отчёт

- [ ] 5.1 Провести вердикт первой ступени в `MarginLineReport` и `SheetPhotoDiagnostics` (решение 7): принят, отвергнут вето (с отношением и связавшим порогом), не звался — факт вызова отдельным полем. Проверка: юнит-тест на листе из 3.1 читает «отвергнут» с числами, на ровном листе с прямой чертой — «принят».
- [ ] 5.2 Печатать вердикт в строке «линия поля» `scripts/sheet-photo-report.ts`. Проверка: `npm run measure:sheet -- paper_photos_examples/IMG_1813.jpeg grid` печатает, какая ступень что решила.

## 6. Пресет-пак

- [ ] 6.1 Прогнать `npm run build:paper`. Проверка: `git diff --stat public/paper/profiles.json` пуст и `md5 -q public/paper/profiles.json` печатает `bbb9e9b963bc092e50aad90f60fd39ce`. Расхождение — правка задела работающий путь и возвращается на доработку.

## 7. Приёмка по предмету issue

- [ ] 7.1 Прогнать `npm run measure:sheet -- paper_photos_examples/IMG_1813.jpeg grid`. Проверка: линии поля справа нет; она либо не найдена, либо найдена `left` и отстоит от самого внутреннего `u` из 1.5 (401) не больше чем на 0,2 шага; если найдена — цвет её столбца по скрипту из 1.2 даёт `R > G`.
- [ ] 7.2 Проверить блок текста юнит-тестом: разлиновку `IMG_1813` после правки выгрузить дампом `SheetRuling` в JSON (из `measureSheetPhoto` + `buildSheetRuling` тем же бандлом, что у отчёта: отчёт не печатает коэффициенты перспективы и узлы изгиба, и собрать фикстуру из него нельзя), положить фикстурой и посчитать `deriveGeometry`. Проверка: ширина блока не меньше ширины между полями листа за вычетом линии поля (если найдена) — блок больше не кончается на 1640 px; если черта найдена, левый край блока отстоит от неё внутрь не меньше чем на 0,2 шага.
- [ ] 7.3 Тот же тест поверх `mirrorSheetRuling`. Проверка: линия поля (если есть) у противоположной стороны, блок не сужен до половины листа и не заходит за черту.
- [ ] 7.4 Сторож `IMG_1812`. Проверка: `npm run measure:sheet -- paper_photos_examples/IMG_1812.jpeg grid` печатает `473.9 px, left`, фантома справа нет.
- [ ] 7.5 `IMG_1803`. Проверка: `npm run measure:sheet -- paper_photos_examples/IMG_1803.jpeg grid` не печатает линию около 111,7; линия либо не найдена, либо найдена `left` около 640 px и не дальше 0,2 шага от самого внутреннего `u` из 1.5, а цвет её столбца по скрипту из 1.2 даёт `R > G` и ΔR < ΔG.

## 8. Неухудшение

- [ ] 8.1 Прогнать отчёт по всем 26 снимкам и сверить с разметкой цветом из 1.5 и с опорой 1.3. Проверка: все одиннадцать настоящих линий на месте до печатаемого знака (или сдвинуты не больше 0,2 шага с записанной причиной); шаг, фаза, угол, перспектива и поля не изменились, кроме снимков, где снята ложная линия (`IMG_1813`, `IMG_1803`, `IMG_1705`) или найдена пропущенная (`IMG_1808`). Появление линии на `IMG_1808` — выигрыш по разметке цветом, а не нарушение неухудшения.
- [ ] 8.2 Проверить фантомы против разметки 1.5. Проверка: ни один снимок класса «черты нет» линию не получил; каждая линия, появившаяся, сменившая сторону или положение, совпадает с разметкой по стороне и в пределах 0,2 шага по `u`. У `IMG_1596` линии нет и барьер не меньше чем в 1,5 раза выше лучшего кандидата. `IMG_1705` — линии нет (не 2798,5 right); `IMG_1808` — линия right не дальше 0,2 шага от `u` 2305 (не 2642,7), цвет её столбца по скрипту из 1.2 даёт `R > G`.
- [ ] 8.3 Если механизм сдвинул барьер или состав соседей — перемерить таблицу калибровки #9 (минимум по проходам, класс по разметке 1.5). Проверка: запас не меньше 0,1 в обе стороны, отдельной строкой запас `IMG_1706`. Если не сдвинул — записать в журнал, почему таблица не перемеряется.
- [ ] 8.4 Сверить время медианой трёх прогонов против 1.4 (порог подтверждён координатором), с вето по цвету и удешевлением 4.12. Проверка: ни один снимок и ни один пресет не вырос больше чем на 3 с абсолютно; рост больше 20 % записан в журнал числом для тела PR.

## 9. Синтетическая удержанная выборка

- [ ] 9.1 После всех правок кода координатор (не исполнитель правки) выбирает seed, записывает его в журнал и разыгрывает двенадцать листов генератором из 3.2. Вскрывается один раз, без правок после прогона. Проверка: ни одного фантома у стороны без черты; каждая найденная черта — в пределах 0,2 шага от самого внутреннего положения. Листы разыгрываются с цветом черты и вставной вертикали (4.11). Провал — повод вернуться к решениям `design.md`, а не подкрутить порог.
- [ ] 9.2 (необязательно: решение координатора — прогон идёт без новых фотографий) Если пользователь всё же дал новые фотографии — прогнать их один раз тем же способом, класс по цвету. Проверка: результат записан в журнал; если фотографий нет — запись «новых снимков нет».

## 10. Гейт и задача целиком

- [ ] 10.1 Прогнать `npm run lint` и `npm test`. Проверка: 0 errors и 0 warnings, все тесты зелёные.
- [ ] 10.2 Прогнать `npm run test:stories`. Проверка: попадание базовых линий и `a11y` зелёные.
- [ ] 10.3 Прогнать `npx openspec validate false-margin-line --strict`. Проверка: `valid`.
- [ ] 10.4 Открыть PR с телом `Closes #10`, приложив диагноз 2.5 и числа 4.12, 6.1, 7.1—7.5, 8.1—8.4, 9.1. Проверка: `gh pr view` показывает связанный issue.
- [ ] 10.5 Аудит ветки против `master` (`review-staged`), находки inline-комментами, правка и резолв до пустого списка. Проверка: GraphQL-запрос нерезолвнутых тредов возвращает пусто.
